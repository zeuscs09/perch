// ข้อมูลประจำเอเจนต์ที่ไม่ใช่เรื่องการวาด
//
// รูปทรงกับจานสีย้ายไปอยู่ที่ pch_mascot.c แล้ว ตอนที่ Codex เลิกเป็น "มาสคอต Claude
// ทาสีฟ้า" แล้วกลายเป็นคนละรูปทรง — การแก้สีของ rect ที่วาดเสร็จแล้วเปลี่ยนจำนวนขาไม่ได้
// และการมีตารางสีอยู่สองที่ย่อมมีวันที่มันไม่ตรงกัน
#pragma once

#include "pch_model.h"

// ชื่อสั้นสำหรับป้ายบนจอ ("Claude" / "Codex" / "AG")
const char *pch_agent_label(pch_agent_t agent);
