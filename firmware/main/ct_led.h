// RGB LED บนบอร์ด — ไฟบอกสถานะที่อ่านได้จากหางตา
//
// จอตอบว่า "เกิดอะไรขึ้นบ้าง" ซึ่งต้องหันไปอ่าน ส่วนไฟตอบคำถามเดียว:
// "ต้องลุกไปทำอะไรไหม" คำถามนั้นต้องตอบได้โดยไม่ต้องหันหน้า ไฟจึงไม่พยายาม
// บอกอะไรมากกว่าหนึ่งอย่าง
//
// ใช้ PWM ไม่ใช่เปิด/ปิดล้วน — เปิด/ปิดได้แค่ 7 สีและกะพริบแข็งๆ ซึ่งบนโต๊ะทำงาน
// อ่านเป็นสัญญาณเตือนตลอดเวลา ส่วนการหายใจช้าๆ อยู่ร่วมกับคนได้ทั้งวัน
#pragma once

#include <stdbool.h>

#include "ct_model.h"

void ct_led_init(void);

// บอกไฟว่าภาพรวมตอนนี้เป็นยังไง — เรียกทุกครั้งที่ snapshot เปลี่ยน
// ไม่ต่อลิงก์ = ดับ (ไฟที่ยังสวยอยู่ตอนขาดการติดต่อคือการโกหก)
void ct_led_apply(const ct_snapshot_t *snap, bool connected);

// เดินจังหวะหายใจ เรียกจากลูปหลักพร้อมเวลาที่ผ่านไป (ms)
void ct_led_tick(int elapsed_ms);
