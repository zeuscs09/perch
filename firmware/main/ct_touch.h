// การแตะจอ — รู้แค่ "มีนิ้วไหม" ไม่รู้ว่าแตะตรงไหน
//
// จอมีตัวควบคุมสัมผัส XPT2046 อยู่บน SPI คนละบัสกับจอ แต่ไฟล์นี้ไม่คุยกับมันทาง SPI เลย
// อ่านแค่ขา PENIRQ ซึ่งเป็นตัวเปรียบเทียบในฮาร์ดแวร์ที่ลงต่ำเมื่อมีแรงกดบนแผ่นสัมผัส
//
// เหตุผลที่พอ: สิ่งเดียวที่บอร์ดนี้ต้องรู้คือ "มีคนอยู่ตรงหน้าไหม" เพื่อปลุกไฟหลัง
// พิกัดไม่ได้ตอบคำถามนั้นเพิ่มเลย แต่พาบัสที่สอง การคาลิเบรต และโอกาสชนกับ SPI ของจอ
// เข้ามาด้วยทั้งชุด — ต้นทุนที่จ่ายไปโดยไม่ได้อะไรกลับมา
//
// ถ้าวันหนึ่งต้องการปุ่มบนจอจริงๆ ค่อยเพิ่มการอ่าน SPI ทีหลัง ขา PENIRQ ยังใช้ได้เหมือนเดิม
#pragma once

#include <stdbool.h>

void ct_touch_init(void);

// เรียกจากลูปหลักพร้อมเวลาที่ผ่านไป (ms) — คืน true ครั้งเดียวต่อการแตะหนึ่งครั้ง
//
// คืนตอน "นิ้วแตะลง" ไม่ใช่ตอนปล่อย เพราะไฟหลังควรติดทันทีที่แตะ ไม่ใช่หลังยกนิ้ว
bool ct_touch_tapped(int elapsed_ms);

// เคยแตะติดสักครั้งตั้งแต่บูตไหม
//
// มีไว้ให้การหรี่จอถามก่อนหรี่: อย่าดับสิ่งที่ตัวเองเปิดคืนไม่ได้ บอร์ดที่ pull-up หาย
// หรือคนละล็อตที่ไม่มีตัวควบคุมสัมผัส จะได้จอที่หรี่แล้วสว่างกลับไม่ได้เลย —
// ซึ่งแยกไม่ออกจากเครื่องพัง การรอให้เห็นการแตะจริงก่อนคือการพิสูจน์ว่ามีทางกลับ
bool ct_touch_seen(void);
