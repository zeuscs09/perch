// หรี่ไฟหลังเองเมื่อไม่มีใครดู
//
// ไฟหลังคือสิ่งที่กินไฟมากที่สุดบนบอร์ดนี้ — เกินครึ่งของทั้งเครื่องตอนเปิดเต็ม
// บนสาย USB มันไม่สำคัญ แต่บนแบตมันคือความต่างระหว่างใช้ได้ทั้งวันกับใช้ได้ครึ่งวัน
//
// ## จอกับไฟตอบคนละคำถาม และนั่นเป็นเหตุผลที่จอหรี่ได้
//
// ไฟ RGB ตอบว่า "ต้องลุกไปทำอะไรไหม" — ต้องเห็นตลอดเวลา และมันกินไฟแทบไม่มี
// จอตอบว่า "เกิดอะไรขึ้นบ้าง" — เป็นคำถามที่ถามตอนเดินกลับมาที่โต๊ะ ไม่ใช่ตลอดเวลา
//
// จอจึงหรี่ได้แม้ยังมี session ที่รอคนตอบอยู่ เพราะไฟยังเต้นบอกอยู่ ส่วนรายละเอียด
// รอให้คนมาถึงก่อนค่อยสว่างก็ทัน
//
// เคยลองให้จอสว่างเองเมื่อมี session ที่ต้องการคน *เพิ่งเกิดขึ้น* แล้วพบว่าบนเครื่อง
// ที่รันหลายสิบทีม เหตุการณ์นั้นเกิดแทบตลอดเวลา จอจึงไม่เคยได้หรี่เลยสักครั้ง —
// วัดจากเครื่องจริง: ตัวนับเวลาว่างถูกรีเซ็ตทุกไม่กี่วินาที ยิ่งเครื่องยุ่ง ยิ่งไม่ประหยัด
// ซึ่งกลับหัวกับสิ่งที่ฟีเจอร์นี้มีไว้ทำ
//
// สิ่งเดียวที่ปลุกจอจึงเป็นการกระทำของคน: แตะจอ หรือลากแถบความสว่าง
#pragma once

#include <stdbool.h>

void pch_dim_init(void);

// ความสว่างที่ผู้ใช้ตั้งจากหน้า Preferences — เพดานของทุกอย่างในไฟล์นี้
void pch_dim_set_user(int percent);

// มีเหตุให้ต้องสว่างเดี๋ยวนี้ (แตะจอ) — สว่างทันที ไม่ค่อยๆ ไล่ขึ้น
void pch_dim_wake(void);

// โหมดกลางคืน — หรี่ลงต่ำกว่าการหรี่ปกติมาก และไม่ต้องรอให้แตะติดก่อน
// เพราะมันมีทางออกที่ไม่ขึ้นกับฮาร์ดแวร์: เช้าแล้วสว่างกลับเอง
void pch_dim_set_night(bool on);

// เดินนาฬิกาและสั่งไฟหลัง เรียกจากลูปหลัก
void pch_dim_tick(int elapsed_ms);
