# Tamaclaude (CYD) — ข้อตกลงการออกแบบ

อุปกรณ์ตั้งโต๊ะที่แสดงสถานะ Claude Code session แบบเรียลไทม์ผ่านมาสคอต Claude
ฮาร์ดแวร์: ESP32-2432S028R ("Cheap Yellow Display")

เอกสารนี้บันทึก **สภาพปัจจุบัน + เหตุผลที่มองไม่เห็นจากโค้ด** เท่านั้น
ประวัติว่าอะไรเคยเป็นอะไรอยู่ใน git log — ไม่ต้องเขียนซ้ำที่นี่
เขียนเพิ่มเมื่อ *กลับคำ* ข้อตกลงเดิม หรือเจอข้อจำกัดที่โค้ดไม่ได้บอก

## ตัดสินใจแล้ว

| หัวข้อ | ผล |
|---|---|
| Firmware | ESP-IDF เขียนใหม่ทั้งหมด (ไม่ fork upstream) |
| Transport | BLE (NimBLE) เป็นทางหลัก + WiFi station เป็นทางสำรองบน LAN (ดู "WiFi") |
| จอ | ILI9341 320x240 แนวนอน |
| กราฟิกสแตก | LVGL v9 + custom draw widget 1 ตัวต่อ 1 slot |
| มาสคอต | ตัวละคร Claude สีส้ม `#D97757` บล็อคสี่เหลี่ยม |
| ผลิตกราฟิก | สคริปต์ Python generate แบบ procedural |
| รูปแบบ asset | rect list `{x,y,w,h,color}` — ไม่มีบิตแมป ไม่มี sprite pipeline |
| สื่อสถานะ | props (ตามเครื่องมือ) + eye state/ลำตัว (ตามอารมณ์) แยก 2 มิติ |
| ฝั่ง Mac | Swift binary เดียว: `--hook` / `--daemon` / menu bar app |
| tool → animation | daemon แปล (config file แก้ได้) firmware รู้แค่ enum คงที่ |
| โปรโตคอล | JSON snapshot ก้อนเดียว เขียนเข้า GATT characteristic |
| สถานะว่าง | มาสคอตนอน + นาฬิกา (ความสว่างคงที่ — ไม่หรี่อัตโนมัติ) |
| BLE หลุด | มาสคอตเปลี่ยนเป็นสีเทา (ไม่มีไอคอน ไม่มีข้อความ) |
| เกณฑ์เตือน | `Notification`/`StopFailure` = การ์ดแดง · `Stop` ที่เงียบเกิน ~45 วิ = การ์ด `done` |
| Slot | สูงสุด 3 ระยะห่างคงที่ 106px ยกทั้งกลุ่มไว้กึ่งกลาง เกินนับ "+N" แต่ alert ยังทะลุถึงได้ |
| Dev loop | Python เรนเดอร์จอทั้งใบเป็น PNG/GIF · layout constants gen จากไฟล์เดียว |
| โควตา | สองแหล่ง: ท่อ statusline (ไม่ต้องมี credential) + ท่อ poll (`sessionKey`) |
| พื้นที่ล่าง | ลำดับ: การ์ด > โควตา > นาฬิกาใหญ่ · การ์ดสูงสุด 2 ใบ ที่เหลือเป็น "+N more" · หลุดลิงก์ = เหลือนาฬิกา |
| ท้องฟ้า | ฟ้า 22..119 + พื้นดิน 120..240 (ครึ่งจอ) · กอหญ้าเกาะเส้นขอบฟ้าแถวเดียว |
| ช่วงเวลา | 4 ช่วงกระโดดที่ขอบ: night 19–05 · dawn 05–07 · day 07–17 · dusk 17–19 |
| แหล่งเวลา | parse ชั่วโมงจาก `snapshot.clock` ที่มีอยู่แล้ว — ไม่มีอะไรเพิ่มบนสาย BLE |
| ฟ้าตอนหลุดลิงก์ | ไม่วาดเลย กลับเป็นพื้น `bg` เรียบ — clock ที่ค้างไม่ใช่เวลาจริงอีกต่อไป |

## v1 scope

**อยู่ใน**: hooks → daemon → BLE → มาสคอตขยับตามเครื่องมือ · multi-session 3 ตัว ·
notification card · auto-reconnect · RGB LED กระพริบ · macOS menu bar app · config ผ่าน BLE ·
นาฬิกา sync จาก host

**ไม่อยู่ใน**: simulator SDL2 · เสียง · microSD · OTA · flash encryption

touch เคยอยู่ในรายการนี้ — **กลับคำแล้ว** แต่เอาแค่ "มีนิ้วไหม" ไม่เอาพิกัด: มันตอบสอง
คำถามที่คุ้มค่าทันที (หรี่จอตอนไม่มีใครดูเพื่อยืดแบต และเรียกดูแผงโควตาที่ปกติโผล่เฉพาะ
ตอนไม่มีการ์ด) โดยไม่ต้องคาลิเบรตและไม่ต้องมีปุ่มบนจอ ดูหัวข้อ "การแตะจอ"

WiFi เคยอยู่ในรายการนี้ — **กลับคำแล้ว** เพราะ BLE หายทันทีที่เดินออกจากโต๊ะ ทำให้จอค้าง
ทั้งที่ Mac ยังทำงานอยู่ ดูหัวข้อ "WiFi" ว่าเปิดแค่ไหนและไม่เปิดอะไร

**ยังไม่ได้ทำ**: เก็บสถานะข้ามการรีสตาร์ท daemon · ปรับ session timeout จากเมนู ·
แจกแบบ notarized (ต้องมีบัญชีนักพัฒนา)

## WiFi

**บอร์ดไม่เคยยิง claude.ai เอง** — WiFi มีไว้ให้ Mac ส่ง snapshot ผ่าน LAN เมื่อ BLE
ไกลเกินไป ไม่ใช่ให้บอร์ดไปดึงโควตาเอง เหตุผลคือ `sessionKey` เป็น credential เต็มบัญชี
ส่วนแฟลชบนบอร์ดไม่ได้เข้ารหัสและดัมพ์ออกได้ด้วยสาย USB (`esptool read_flash`) การย้าย
กุญแจไปไว้ที่นั่นคือการเปลี่ยนของที่หายแล้วต้องเปลี่ยนรหัสผ่านให้กลายเป็นของที่หยิบไปได้

ผลที่ตามมาที่ต้องยอมรับ: **Mac หลับ = จอค้างเหมือนเดิม** ทั้ง BLE และ LAN เงียบพร้อมกัน
ไอคอนบนแถบบนบอกสถานะนี้ตรงๆ แทนที่จะปล่อยให้ภาพเก่าดูเหมือนของสด

| หัวข้อ | ผล |
|---|---|
| ใครสแกนหาเครือข่าย | บอร์ด ไม่ใช่ Mac — ESP32 รับได้แค่ 2.4GHz และตั้งอยู่คนละที่กับ Mac รายการของ Mac จะมีวงที่บอร์ดเข้าไม่ได้ปนมา และ CoreWLAN ยังต้องขอสิทธิ์ Location เพิ่มอีกตัว |
| รายการเดินทางยังไง | หนึ่ง SSID ต่อหนึ่ง notification ปิดท้ายด้วย `{"t":"ap_end"}` — ไม่มี reassembly ไม่มีหมายเลขชิ้น ไม่มี timeout ให้พัง |
| คำสั่งเดินทางยังไง | config characteristic ซึ่งประกาศ `BLE_GATT_CHR_F_WRITE_ENC` — รหัส WiFi ของผู้ใช้ผ่านทางนั้น การจับคู่เป็น Just Works (บอร์ดไม่มีปุ่มให้ยืนยันเลขหกหลัก) กันคนดักฟังผ่านๆ ได้ ไม่กัน MITM ที่อยู่ตรงนั้นพอดีตอนกด Connect |
| จำได้กี่วง | 5 เลือกตัวที่สัญญาณแรงที่สุดในบรรดาที่จำไว้ เต็มแล้วตัวเก่าสุดหลุด |
| ต่อไม่ติด | ลองใหม่เรื่อยๆ backoff 1→60 วิ ไม่มีการเลิก — บอร์ดอยู่บนโต๊ะทั้งวันและไม่มีใครมากดปุ่ม |
| แฟลชไม่เข้ารหัส | ยอมรับ ไม่เปิด flash encryption: มัน burn eFuse ถาวรและทำให้ flash ผ่าน USB ยุ่ง ซึ่งเป็นช่องอัปเดตเดียวที่มี (ไม่มี OTA) ของที่เสียหากบอร์ดหาย = รหัส WiFi กับ PSK ที่เพิกถอนได้ |
| IRAM | `CONFIG_ESP_WIFI_IRAM_OPT=n` + `..._RX_IRAM_OPT=n` — IRAM ของ ESP32 มีก้อนเดียวและ BLE controller จองไปเกือบหมด ไม่ปิดสองตัวนี้แล้วลิงก์ไม่ผ่าน (`iram0_0_seg overflowed`) |

### ทางเดินที่สอง: snapshot บน LAN

BLE ยังเป็นทางหลักเสมอ ทาง LAN เปิดต่อเมื่อ BLE เงียบเกิน **10 วินาที** — การหลุดสั้นๆ
เกิดเป็นปกติ (เดินผ่านตัวเครื่อง, Mac หลับครึ่งหนึ่ง) และ CoreBluetooth ต่อกลับเองได้
การกระโดดไป LAN ทุกครั้งแปลว่าเปิดปิดซ็อกเก็ตทั้งวันเพื่อสิ่งที่หายเองอยู่แล้ว

| หัวข้อ | ผล |
|---|---|
| ใครเปิด server | บอร์ด (พอร์ต 7333) Mac เป็นฝ่ายต่อเข้า — Mac ย้ายที่/เปลี่ยน IP บ่อยกว่าบอร์ดที่นั่งอยู่บนโต๊ะ |
| หาบอร์ดยังไง | ที่อยู่ที่ผู้ใช้กรอกเอง → mDNS `_tamaclaude._tcp` → IP ล่าสุดที่บอร์ดรายงานมาทาง BLE · สามชั้นเพราะ mDNS เป็นสิ่งแรกที่หายเมื่อมี VLAN แขกหรือเราเตอร์กรอง multicast |
| รูปเฟรม | `[4B len BE][12B nonce][ciphertext][16B tag]` AES-256-GCM · nonce = ศูนย์ 4 ไบต์ + ตัวนับ 8 ไบต์ BE |
| ทำไมต้องเข้ารหัสบน LAN ตัวเอง | WPA2 ปิดแค่ช่วงอากาศ ทุกเครื่องบนเราเตอร์เดียวกันอ่านและ**เขียน**ได้ · snapshot มีชื่อโปรเจกต์กับชื่อไฟล์อยู่ข้างใน และช่องที่เขียนอะไรลงจอบนโต๊ะได้โดยไม่ต้องพิสูจน์ตัวคือช่องที่ไม่ควรมี |
| กุญแจมาจากไหน | Mac สุ่ม 32 ไบต์เก็บที่ `~/.tamaclaude/lan-key` (mode 600) แล้วผลักไปบอร์ดทาง config characteristic ที่บังคับเข้ารหัสอยู่แล้ว — ที่เดียวกับรหัส WiFi · ไม่ให้ผู้ใช้ตั้งเอง เพราะรหัสที่คนตั้งเองสั้นพอให้เดาได้ทุกครั้ง |
| รู้ได้ไงว่ากุญแจตรงกัน | บอร์ดส่งลายนิ้วมือ (8 hex แรกของ SHA-256) มากับสถานะ WiFi · ไม่ตรงเมื่อไร Mac ผลักของใหม่ไปเองทันที ผู้ใช้ไม่มีทางรู้ว่าตัวเลขสองชุดนี้ต่างกันอยู่ |
| กันเล่นซ้ำ | ตัวนับใน nonce ต้องมากกว่าตัวที่รับไปแล้วเสมอ · **บอร์ดบอกเลขนั้นให้ Mac ตอนรับสาย** (คำทักทาย 13 ไบต์) Mac จึงไม่ต้องเก็บไฟล์ตัวนับเอง — ไฟล์แบบนั้นหายเมื่อไรก็แปลว่าต่อไม่ได้อีกจนกว่าจะเปลี่ยนกุญแจ ซึ่งเป็นความพังที่ไม่มีอาการให้เห็น · คำทักทายไม่ได้ถูกยืนยันตัว แต่การโกหกมันทำได้แค่ดันตัวนับให้สูงขึ้น ไม่เปิดทางให้เฟรมเก่าผ่าน |
| จด NVS ทุกกี่เฟรม | ทุก 256 (`FLOOR_STRIDE`) ไม่ใช่ทุกเฟรม — snapshot เดินทางทุกไม่กี่วินาที การเขียน flash ทุกครั้งกินอายุเซกเตอร์หมดในไม่กี่เดือน ราคาคือหลังไฟดับตัวนับกระโดดไปข้างหน้าไม่เกิน 256 |
| เฟรมยืนยันตัวไม่ผ่าน | ตัดสาย ไม่ใช่ข้ามเฟรมนั้น — ความยาวที่อ่านผิดไปหนึ่งไบต์ทำให้ทุกเฟรมหลังจากนั้นเพี้ยนหมด และ Mac ต่อกลับเองพร้อมรับคำทักทายใหม่ ตัวนับจึงซิงก์ตัวเองโดยไม่มีใครต้องทำอะไร |
| เพดานขนาด | คง 500 ไบต์เท่า BLE — สายเส้นเดียวสองทางเดินที่บีบข้อความคนละเกณฑ์แปลว่าจอแสดงคนละอย่างขึ้นกับว่าตอนนั้นเดินทางไหน |
| ทิศทาง | ทางเดียว (Mac → บอร์ด) ยกเว้นคำทักทาย · ผลสแกน WiFi กับสถานะยังเดินทางบน BLE เท่านั้น เพราะมันมีความหมายก็ต่อเมื่อผู้ใช้กางหน้าตั้งค่าอยู่ ซึ่งเป็นตอนที่ BLE ต้องติดอยู่แล้ว |
| จอบอกอะไร | เหลือแต่ WiFi = ป้ายบนแถบเปลี่ยนจากชื่อบอร์ดเป็น **IP** — เป็นตอนเดียวที่ผู้ใช้ต้องใช้เลขนั้น (Mac หาไม่เจอ ต้องกรอกเอง) และเป็นตอนที่เขาอ่านจากหน้าจอ Mac ไม่ได้แล้ว |
| Local Network | `NSLocalNetworkUsageDescription` + `NSBonjourServices` ใน Info.plist · macOS 15 ขึ้นไปขออนุญาตแยกจาก Bluetooth และการขาดคีย์ทำให้ทั้ง Bonjour และการต่อไปหา IP ตรงๆ เงียบไปโดยไม่มี error |

## ฮาร์ดแวร์ (วัดจากบอร์ดจริง)

ESP32-D0WD-V3 rev 3.1 · 4MB flash · ไม่มี PSRAM · พอร์ต `/dev/cu.usbserial-*`
MAC 2 ไบต์ท้ายคือชื่อที่บอร์ดใช้โฆษณาบน BLE

ผลตรวจจอด้วย `firmware/probe/`:

```
MADCTL          0x28   = MV | BGR   -> แนวนอน 320x240 origin มุมบนซ้าย
ลำดับสี         BGR    ไม่ใช่ RGB
Inversion       ปิด    (INVOFF)
COLMOD          0x55   16 bit/pixel
```

```
TFT (SPI2):   MOSI 13  MISO 12  SCLK 14  CS 15  DC 2  RST -1  BL 21   [ยืนยันแล้ว]
RGB LED:      R 4   G 16   B 17          (active low)                  [ยืนยันแล้ว]
Touch XPT2046 (SPI3):     CLK 25  MOSI 32  MISO 39  CS 33  IRQ 36     [ยืนยันแล้ว]
microSD:      CS 5  MOSI 23  MISO 19  SCK 18                          [ไม่ใช้]
Speaker:      GPIO 26 (DAC1)                                          [v2]
```

RAM ที่เหลือจริงตอนทำงาน (BLE + WiFi + LAN + mDNS + LVGL ขึ้นครบ อยู่บนทาง LAN สิบนาที):

```
idf.py size      DRAM static เหลือ 24060 · IRAM เหลือ 22105
esp_get_free_heap_size()          ~30 KB
esp_get_minimum_free_heap_size()  ~23 KB   <- ก้นจริง ถึงตอนนาทีที่ 3 แล้วไม่ลงอีก
```

**กรอบที่ต้องคิดคือ 23KB ไม่ใช่ 61KB ที่ `heap_init` โฆษณาตอนบูต** — WiFi กับ LAN จอง
หลังบรรทัดนั้น ตัวเลขตอนบูตจึงมองโลกในแง่ดีเกินจริงเกือบสามเท่า ของที่จองก้อนโตกว่านี้
ต้องวัดใหม่ ไม่ใช่เดาจาก `idf.py size` อย่างเดียว · `main.c` จึงพ่น `heap`/`min` นาทีละ
บรรทัดไว้ถาวร: ที่รั่วช้าๆ ไม่มีอาการอะไรเลยจนกว่าจะจองไม่ผ่านแล้วบอร์ดรีบูตทิ้ง

**เลขรุ่นชิปพิสูจน์ไม่ได้ และไม่จำเป็นต้องพิสูจน์** — จอล็อกการอ่าน ID (`0x04`, `0xD3`,
`0xDA/DB/DC` คืนศูนย์) มีแต่ `0x0A` ที่ตอบ `0x98` ซึ่งเหมือนกันทั้ง ILI9341 และ ST7789
พฤติกรรมชี้ไป ILI9341 (inversion ต้องปิด ส่วน ST7789 เกือบทั้งหมดต้องเปิด)
**firmware ใช้ค่าคอนฟิกด้านบน ไม่ใช่เลขรุ่น**

## การแตะจอ

รู้แค่ "มีนิ้วไหม" ไม่รู้ว่าแตะตรงไหน — และนั่นเป็นการตัดสินใจ ไม่ใช่ข้อจำกัด

พิกัดไม่ได้ตอบคำถามที่บอร์ดนี้ถามเพิ่มเลย แต่พาการคาลิเบรต ปุ่มบนจอ 2.8 นิ้ว และ
โอกาสชนกับ SPI ของจอเข้ามาทั้งชุด ส่วน "มีคนอยู่ตรงหน้าไหม" ตอบได้ด้วยขาเดียว

**แตะหนึ่งครั้งได้สองอย่างพร้อมกัน** ซึ่งเป็นสองสิ่งที่คนเดินมาถึงโต๊ะอยากได้พร้อมกันพอดี:

1. **ปลุกไฟหลัง** — สว่างทันทีไม่ไล่ขึ้น (คนที่เพิ่งแตะกำลังรอดู)
2. **เรียกดูแผงโควตา 8 วินาที** — ปกติแผงนี้โผล่เฉพาะตอนไม่มีการ์ดค้าง ซึ่งเป็นจังหวะ
   ที่ผู้ใช้เลือกไม่ได้ อยากรู้ว่าเหลือโควตาเท่าไรตอนไหนก็ต้องรอให้การ์ดหมดไปเอง

### กับดักที่เสียเวลาไปกับมัน

**XPT2046 ไม่ปล่อยสัญญาณ PENIRQ มาตั้งแต่เปิดเครื่อง** มันปล่อยเมื่ออยู่ในโหมด
power-down ที่เปิด PENIRQ ซึ่งเกิดหลังจบการแปลงค่าที่สั่งด้วย `PD1=PD0=0` เท่านั้น
ถ้าไม่เคยส่งคำสั่งอะไรเลย ขาจะค้างสูงตลอดกาลและการแตะเงียบสนิท (วัดได้ `low=0/50`
ทุกรอบไม่ว่าจะแตะหรือไม่) จึงต้องส่ง `0x90` ครั้งเดียวตอนบูตเพื่อปลุกชิป —
เป็นเหตุผลเดียวที่โมดูลนี้ต้องแตะ SPI เลย หลังจากนั้นอ่านแต่ขา GPIO

**GPIO 36 ไม่มี pull-up ในชิป** (ขา 34-39 ของ ESP32 ไม่มีทั้ง pull-up และ pull-down)
ต้องพึ่งตัวบนบอร์ด มีตัวตรวจตอนบูตที่ร้องออกมาถ้าขาลอย

### จอไม่หรี่จนกว่าจะแตะติดสักครั้ง

อย่าดับสิ่งที่ตัวเองเปิดคืนไม่ได้ บอร์ดที่ pull-up หายหรือคนละล็อตที่ไม่มีตัวควบคุมสัมผัส
จะได้จอที่หรี่แล้วสว่างกลับไม่ได้ ซึ่งแยกไม่ออกจากเครื่องพัง การรอให้เห็นการแตะจริง
ก่อนคือการพิสูจน์ว่ามีทางกลับ

## ตัดสินใจเชิงเทคนิค

- **NimBLE** ไม่ใช่ Bluedroid — เล็กกว่า และต้องการแค่ BLE peripheral
- **ไม่มี OTA ใน v1** — partition เป็น factory app ก้อนเดียว + NVS ได้ app เต็ม ~3.5MB
  จาก 4MB (มี OTA จะเหลือ slot ละ ~1.7MB) แฟลชผ่าน USB อยู่แล้วตอนพัฒนา
- **LVGL partial buffer** 2 ก้อน ก้อนละ 1/10 จอ (~15KB) ไม่ใช่ full framebuffer 150KB
- **daemon truncate ทุก string** ก่อนส่ง ให้ snapshot พอดี 1 MTU — ไม่มี chunking
- **ฟอนต์บนบอร์ดไม่มี em dash และไม่มีอักขระไทย** — daemon ต้องกรองข้อความให้เหลือ
  เฉพาะชุดอักขระที่ฟอนต์มีจริงก่อนส่ง (`Text.swift`) ไม่งั้นได้กล่องสี่เหลี่ยม

## ภาพบนจอ

หลักที่ใช้ตัดสินทุกข้อ: เรนเดอร์จอ 320x240 จริงแล้วดู ไม่ตัดสินจากการคิดเอา

**สัดส่วน**

- **สัดส่วนตัวมาสคอตมาจากตาราง 12 x 8 ช่องของภาพอ้างอิงตรงๆ** ช่องละ 1.5 unit
  (ลำตัว 8x8 · แขนข้างละ 2x2 · ตา 1x1 · ขากว้าง 1 สูง 2 ที่ช่อง 0/2/5/7) — เดิมเป็นตัวเลข
  ที่ปรับทีละส่วนจนลำตัวกว้างเกินสัดส่วน (82% ของซิลลูเอ็ต แทนที่จะเป็น 67%) และแขนสั้นเกินครึ่ง
  ทุกค่าจึงต้องมาจาก `CELL` ไม่ใช่ตัวเลขดิบ ไม่งั้นความเพี้ยนกลับมาสะสมอีก
  · **ข้อเดียวที่จงใจเบี่ยงคือแขน: 1.5 ช่อง ไม่ใช่ 2** — ที่ 4 px/unit แขนเต็มสองช่องยื่นออก
  12 px ต่อข้าง อ่านเป็นแขนยาวเก้งก้าง ไม่ใช่ตอแขนแบบต้นฉบับ (ต้นฉบับถูกดูที่ขนาดใหญ่กว่านี้มาก)
- ซิลลูเอ็ต 16.5 unit + พื้นที่ prop ถึง 23.4 unit ที่ `unit_px = 4` = 95.6px ใน slot 106px
  prop ที่ล้นเกินนี้จะไปตกบนพื้นหลัง slot ถัดไปแล้วอ่านเป็นของตัวข้างๆ · ที่โตได้แค่นี้เพราะ
  **แนวนอนคือด้านที่ตัน ไม่ใช่แนวตั้ง** — ช่อง prop ที่ถือกิน 6.2 unit ตายตัว โตกว่านี้ต้องแลกกับมัน
- **3 slot ไม่ใช่ 4** — 4 ตัวบนจอ 320px แปลว่าตัวละไม่เกิน 80px ซึ่งเล็กเกินกว่าจะดูออก
  จากอีกฝั่งโต๊ะ ทั้งที่ session ที่ 4 ถูกนับ "+N" ให้อยู่แล้ว · `unit_px = 4` คือค่าสูงสุด
  ที่ยังวาง 3 ตัวเรียงได้ และ outline 0.34 unit ยังปัดเป็น 1px
- **slot ยกทั้งกลุ่มไว้กึ่งกลาง** ระยะห่างคงที่ 80px ไม่ยืดหดตามจำนวน
  สิ่งที่ต้องนิ่งคือ **ลำดับซ้าย→ขวา** ไม่ใช่พิกัดสัมบูรณ์ การจัดกลุ่มกึ่งกลางไม่เคยสลับลำดับ
- **ไม่มีเส้นคั่น slot และไม่สลับสีพื้นหลัง slot** — เส้นตั้งเต็มความสูงอ่านเป็นเสาและตัดฉาก
  ผืนเดียวเป็นสามชิ้น ส่วนพื้นหลังสลับสีทำให้ prop อ่านเป็นของ slot ข้างๆ
  หน้าที่กันความสับสนตกเป็นของ **เงาใต้เท้า** ซึ่งบอกว่าแต่ละตัวยืนตรงไหน
- **เส้นขอบฟ้าอยู่กลางจอพอดี (y=120)** — ฟ้าสูง 98px เหนือกรอบวาดมาสคอตโล่ง 28px
  คือทางที่ดวงอาทิตย์เดินโดยไม่โดนบัง (`arc_peak` 82)

**มาสคอต**

- **แยก mood (ตา/ลำตัว) ออกจาก prop (ของที่ถือ) เป็นคนละมิติ** — นี่คือสิ่งที่ทำให้
  11 สถานะแยกออกจากกันได้ทั้งหมดโดยไม่ต้องวาดทีละท่า
- `waiting` = นาฬิกาเหลือง · `alert` = "!" แดง — คนละความหมาย ("รอคุณตอบ" ≠ "พัง")
  ต้องต่างทั้งรูปทรงและสี · `celebrate` ตาเป็น `^ ^` ไม่ใช่ขีดแบน (ซ้ำตาหลับของ `sleeping`)
- **กะพริบตาเดินด้วยนาฬิกาที่ช้ากว่าลูปอนิเมชัน** (`cycle` แยกจาก `phase`) 1 เฟรมทุก 4 ลูป
  ≈ 4.3 วิ — กะพริบทุกวินาทีอ่านเป็นกระวนกระวาย
- **ทุกชิ้นตั้งฉาก** — เส้นทแยงบางๆ ที่ความละเอียดนี้เละเป็นขั้นบันได
- **แว่นขยาย: กระจกกับวงแหวนต้องผ่านทรานส์ฟอร์มชุดเดียวกัน** — กระจกวาดก่อนตา ส่วนวงแหวน
  วาดหลังตา จึงอยู่คนละท่อนของ `build()` แต่มันคือของชิ้นเดียวกัน · กระจกเคยถูก `_squashed()`
  ตามลำตัวขณะที่วงแหวน (เป็น prop) ไม่ถูกยุบ ท่าที่ squash > 0 จึงดันกระจกลง ~1.5px
  แล้วเห็นเนื้อลำตัวโผล่ใต้ขอบบนของวง · **ขนาดเลนส์กับความหนาขอบต้องลงตารางพิกเซลทั้งคู่
  รวมถึงมุมที่ตัด `s/4` และ `s/4 - t`** ไม่งั้นบันไดมุมของสองชิ้นเหลื่อมกันเป็นร่อง
- **ลูกโลก (`searching`) อยู่บนหัว 6.5 unit และลอยนิ่งไม่เด้งตามลำตัว** — ของที่ถือข้างตัว
  อ่านเป็นของที่วางอยู่ข้างๆ ไม่ใช่สิ่งที่มาสคอตกำลังทำ · ตัวเด้งผ่านลูกที่ลอยนิ่งจึงอ่านเป็น
  ลูกโลกลอยจริง · ทรงกลมประกอบจากแถบแนวนอน 13 แถบ (0.5 unit) กว้างตามสมการวงกลม
  **วัดที่กึ่งกลางแถบ ไม่ใช่ขอบ** (วัดที่ขอบได้โดมแบน) · ฟ้า/เขียวคือรหัสสีที่ไม่ต้องอธิบาย
  · ทวีปที่ล้นขอบเล็มเฉพาะส่วนที่ล้น ไม่ซ่อนทั้งก้อน (ก้อนที่หายวับอ่านเป็นกะพริบ)
  · **ลูกมีขอบสี `steel` หนา 2 px** — น้ำสี `glass` (#A8D8EA) กับฟ้ากลางวัน (#B8DCF5)
  ห่างกันไม่ถึงหนึ่งสเต็ป ลูกโลกจึงกลืนหายไปทั้งลูกตอนกลางวัน · แก้ด้วยการเปลี่ยนสีน้ำไม่ได้
  เพราะฟ้ามีสี่ช่วงตั้งแต่ #0B0F1A ถึง #B8DCF5 ไม่มีสีเดียวที่ตัดกับทั้งสี่ · ขอบเข้มกับไส้สว่าง
  ตัดกันคนละทาง อย่างน้อยหนึ่งอย่างจึงเห็นเสมอ (ไส้ได้ 12.5:1 กับกลางคืน ขอบได้ 3.4:1 กับ
  กลางวัน) — **prop ทุกชิ้นที่ลอยอยู่บนฟ้าต้องมีคู่เข้ม/สว่างแบบนี้ ไม่ใช่สีเดียว** · ขอบเป็น
  `steel` ไม่ใช่ `ink`: สีหมึกบนลูกฟ้าอ่อนอ่านเป็นเส้นขีดดำ ไม่ใช่ขอบของทรงกลม และขอบ 1 px
  ขาดๆ หายๆ ตามขั้นบันไดของแถบ ต้อง 2 px = ความสูงของแถบพอดี ขอบจึงหนาเท่ากันทุกด้าน
- **`conducting` (prop `crew`, priority 22)** — subagent ยิง hook ด้วย `session_id` เดียวกับพ่อ
  สล็อตพ่อจึงกระพริบเป็นงานที่ไม่ใช่ของมัน · `subagents > 0` ชนะ tool ทั้งหมด แต่แพ้
  `error`/`waiting` · **priority ต้อง > tool (20)** เพราะ session ที่คุม subagent เงียบสนิท
  ถ้าเท่ากันจะแพ้ tie-break ที่ `lastActivity` แล้วหลุดจอ · ตัวนับต้องรีเซ็ตที่
  `Stop`/`SessionStart`/`SessionEnd`/`UserPromptSubmit` — `SubagentStop` ที่หายไปตอน
  daemon ไม่ได้รันจะทำให้ท่าค้างตลอดกาล · prop เป็นมาสคอตจิ๋วสองตัว ไม่ใช่ไม้กำกับวง
  (สารคือ "มีหลายตัวทำงานอยู่" ไม่ใช่อุปมา)
- **`beacon` (priority 20)** — `LSP` และ `mcp__*` ไม่ได้อ่านดิสก์และไม่ได้ค้นเว็บ
  แต่กำลัง **รอบริการอื่นตอบ** ซึ่งค้างนานที่สุด · เสาอากาศ **บนหัว** (สัญญาณต้องออกจาก
  ตัวมาสคอตเอง) · คลื่นแผ่ออก **ด้านข้าง** ไม่ซ้อนขึ้นบน — เหนือหัวมีที่แค่ 5.6 unit
  แนวนอนมีเหลือเฟือ · ยิ่งไกลสันยิ่งสูง จึงอ่านเป็นวงที่กว้างขึ้น ไม่ใช่ขีดสามขีดยาวเท่ากัน
  · ชั้นโผล่สะสม 1→2→3 แล้ววนใหม่ ชั้นที่ยังไม่ถึงคิวไม่วาดเลย · สีเหลืองสีเดียวทั้งชุด
  (หลายสีอ่านเป็นของหลายชิ้น) · ไฟยอดเสาไม่กะพริบ (จังหวะอยู่ที่คลื่นแล้ว)
  · ยกมือค้างสองข้างเป็นสองท่อน (`arm_up` + `arm_out`) — nub ที่เลื่อนขึ้นเฉยๆ
  อ่านเป็น "ไหล่สูง" และการโยกอ่านเป็นโบกมือ
- **`entering`/`leaving` ใช้ `phase` เป็นความคืบหน้า 0→1 ไม่ใช่ลูปวน**
- **ไม่มี session = มาสคอตเดินเล่น** — เดินจากนอกจอซ้ายไปขวา 34 px/วิ แล้ววนใหม่
  หยุดทำท่า 2.5 วิ กลางทาง วนสี่ท่า (celebrate/thinking/searching/waiting)
  ตำแหน่งหยุดวนสามค่า (0.34/0.5/0.66) — จำนวนไม่เท่ากันจึงไม่จับคู่ซ้ำ ·
  คำนวณจาก `cycle + phase` ที่เดินอยู่แล้ว ไม่ต้องมี state เพิ่ม และไม่ต้องพึ่ง BLE

**ฉากและสี**

- **ฟ้ากลางวันต้องข้ามความสว่างของมาสคอตไปให้พ้น** — มาสคอตอยู่ที่ luminance 0.27
  กราฟ contrast มีก้นบึ้งตรงนั้น (`#7FB4D9` = 0.43 ได้แค่ 1.5:1) `#B8DCF5` (0.67) ได้ 2.2:1
  และทำให้เมฆต้องเป็นสีขาว
- **dawn กับ dusk อยู่คนละวงศ์สี** — เช้า = น้ำเงินคราม · เย็น = แดงอมม่วง
  ทั้งคู่ยาวช่วงละ 2 ชม. และไม่มีวันปรากฏพร้อมกัน ตัวแยกจึงต้องเป็นสีล้วนๆ
  ไม่ใช่ความสว่างที่ต่างกัน · เมฆแยกสีตามด้วย ไม่ใช้สีร่วม
- **ดวงอาทิตย์ขึ้น/ตกที่ขอบจอพอดี (`arc_pad = 0`)** — ให้โผล่พ้นขอบแล้วดวงหายไปนอกจอ
  ตลอด 05:00–05:55 และ 18:05–19:00 ซึ่งเป็นช่วงเดียวที่ต้องเห็นดวงติดขอบฟ้า
- **พื้นดินเข้มทุกช่วง (luminance ~0.04) สีเขียวไปอยู่ที่กอหญ้า** — พื้นดินกินถึงก้นจอ
  ซึ่งมีเลข % และนาฬิกาใหญ่วางทับ ถ้าพื้นเป็นเขียวหญ้าตัวเลขจะจม · กอสูงไม่เท่ากัน
  (วน 3..6 px) กอสูงเท่ากันหมดอ่านเป็นรั้ว · ก้านหนา 2px เว้น 1px — ก้าน 1px หายไปเลย
  บนแผงจริงทั้งที่เห็นชัดบน preview ที่ขยาย 3 เท่า
- **เงาใต้เท้าขนาดคงที่ ไม่ยุบตามความสูง** — เงาที่ยุบตามกลายเป็นสิ่งที่ต้องมอง แทนที่จะ
  ทำให้มองตัวละครถูก · เกาะกึ่งกลาง *ลำตัว* ไม่ใช่กึ่งกลาง slot (`build_centered`
  จัดกึ่งกลางกรอบที่รวม prop) · แยกสีตามช่วงเวลา — ค่าที่เข้มพอสำหรับกลางคืน
  กลายเป็นรูดำบนพื้นกลางวัน

**พื้นที่ล่าง**

- **นาฬิกาใหญ่กลางจอตอนไม่มีการเตือน** — นี่คือสภาพที่จอเป็นอยู่เกือบตลอดเวลา
  ปล่อยว่างแล้วอ่านเหมือนอุปกรณ์พัง
- **สีการ์ดแยกตาม "ต้องมือคนไหม" ไม่ใช่ "มีอะไรเกิดขึ้นไหม"** — `Notification` กับ
  `StopFailure` เป็น `.alert` (แดง) เพราะไม่มีคนแล้วเดินต่อไม่ได้จริง · แต่ `Stop`
  ที่เงียบครบ 45 วิ เป็น `.done` (`"your turn"`) เพราะเทิร์นจบเฉยๆ ไม่มีอะไรค้างให้ตอบ
  ตอนแรกใบนี้เป็น `.alert` เหมือนกัน ผลคือทุกเทิร์นที่ลุกจากโต๊ะได้การ์ดแดง จนแดงเลิกแปลว่า
  "ต้องมือคน" · ท่ามาสคอตยังเป็น `waiting` ทั้งสองทาง — "ถึงตาคุณ" กับ "ต้องตอบเดี๋ยวนี้"
  ต่างกันที่ความเร่ง ไม่ใช่ว่าใครถือลูกอยู่
- **การ์ด 2 ใบ + `m` (จำนวนที่เกิน)** — ใบที่ 3 ดันแผงล่างชนป้ายชื่อโปรเจกต์
  แต่การตัดทิ้งเงียบๆ ทำให้ "ไม่มีอะไรค้างแล้ว" กับ "ยังค้างแต่จอไม่พอ" หน้าตาเหมือนกัน
  · `m` นับรวมใบที่ `Snapshot.encoded` ตัดเพราะ MTU ล้นด้วย — เหตุผลที่การ์ดหายไป
  ไม่สำคัญกับคนอ่านจอ
- **แถบโควตาสองชั้น** (เลข % + เวลารีเซ็ตบรรทัดเดียวกัน แล้วแถบ) = 40px ต่อแถว
  เลข % ยังฟอนต์ 24 ซึ่งเป็นเหตุผลเดียวที่แถวนี้มีอยู่ · แผงที่เตี้ยกว่าพื้นที่จัดกลางแนวตั้ง
  ไม่ชิดบน — ก้นจอที่โล่งเป็นแถบอ่านเป็น "ของหาย" ไม่ใช่การตัดสินใจ
- **มีการ์ด = โควตาย่อขึ้นแถบบน** — แถบ 34x6px + `52%` เฉพาะหน้าต่าง 5 ชม.
  **ไม่มีป้าย `5h`** (บนแถบมีค่าเดียว) countdown ตัดทิ้ง — ตอนมีการ์ดสิ่งที่ต้องทำคือจัดการ
  notification ไม่ใช่วางแผนโควตา · ทั้งชุดหลบตำแหน่งเมื่อ `+N` โผล่

## แผงโควตา

**ที่มาของตัวเลข — สองทาง ไม่ใช่ทางเดียว**

1. **ท่อ statusline** — Claude Code ป้อน JSON เข้า stdin ของคำสั่ง `statusLine` ทุกครั้งที่
   render และมี `rate_limits.five_hour` / `.seven_day` (`used_percentage` 0–100 กับ
   `resets_at` epoch) ไม่ต้องมี credential ไม่ยิงเน็ต ไม่ต้องมี timer
2. **ท่อ poll** — `tamaclaude --usage-poll` ยิง `/api/organizations/<org>/usage` ด้วย
   `sessionKey` ตามรอบที่ menu bar ถือไว้

**การกลับคำ** — เอกสารเดิมเขียนว่าท่อ statusline คือ *แหล่งเดียว* และ "ไม่มี sessionKey
ไม่ยิงเน็ต" ซึ่งไม่จริงอีกต่อไป เหตุผลเดิมยังถูก แต่เราเจอสิ่งที่มันทำไม่ได้: statusline เป็น
**event-driven ล้วน** จึงเงียบตอน session ว่าง ซึ่งคือสภาพที่จอบนโต๊ะเจอเกือบตลอดเวลา
ปิด Claude Code ทิ้งไว้แล้วตัวเลขค้าง — และค้างแบบที่แยกไม่ออกจากค่าที่ยังถูก
ทางแก้เดียวคือถามต้นทางเอง ซึ่งต้องมี credential
ท่อ statusline ยังอยู่เพราะไม่มีอะไรให้หมดอายุ: ผู้ใช้ที่ไม่เคยตั้ง key ก็ยังเห็นตัวเลข
และวันที่ key หมดอายุ ค่าที่ Claude Code ป้อนเข้ามายังถูกต้อง **สองอันทำคนละหน้าที่
และเป็นสวิตช์คนละตัว** — ตั้ง key สำเร็จแล้วไม่ auto-uninstall statusline
(การถอนอัตโนมัติคือการแตะ `settings.json` ของผู้ใช้โดยไม่ได้สั่ง)

**"ไม่แตะ Keychain" ยังไม่กลับคำ** — สิทธิ์เข้าถึงรายการใน Keychain ผูกกับลายเซ็นแอป
และแอปนี้เซ็น adhoc: บิลด์ใหม่ = cdhash ใหม่ = Keychain เด้งถามรหัสเครื่องทุกครั้งที่อัปเดต
แลกกับความปลอดภัยที่มากกว่าไฟล์ 600 เฉพาะกรณีที่ผู้บุกรุกอ่านไฟล์ในบ้านผู้ใช้ได้แต่รันโค้ด
ในนามผู้ใช้ไม่ได้ ซึ่งแคบมาก · ถ้าวันหนึ่งมี Developer ID จริง ข้อนี้ควรกลับคำเป็นอันดับแรก

ข้อจำกัดของท่อ statusline: `rate_limits` มีเฉพาะบัญชี Pro/Max และเฉพาะหลัง API response
แรกของ session · แต่ละหน้าต่างหายไปอิสระต่อกันได้

**ทางเดินของข้อมูล**
```
Claude Code --stdin--> ~/.tamaclaude/statusline.sh --.
                                   |                  \
                          (ส่งต่อให้ statusline เดิม)   >--> ~/.claude/.statusline-usage-cache
                                                      /                  |
claude.ai /api/.../usage --payload--> UsageWriter ---'        daemon อ่าน --> "u" --> บอร์ด
```

**กฎของ cache**

- **ไม่รู้ ≠ ศูนย์** (ADR-0001 ของ `esp32-claude-quota`) — `-1` คือไม่รู้ `0` เป็นค่าจริง
  ไม่เคยมีข้อมูลเลย → ซ่อนแผงทั้งอันกลับไปเป็นนาฬิกา (โครงเปล่าดูเหมือนอุปกรณ์พัง)
  มีทีละตัว → แถวที่ขาดเป็น `--` · countdown ถึง 0 → เปอร์เซ็นต์เป็น `--` ทันที
- **ห้ามเขียนค่าที่เก่ากว่าทับ** — invariant: **ภายในหน้าต่างเดียวกัน (`resets_at` ตรงกัน)
  เปอร์เซ็นต์เพิ่มอย่างเดียว** ค่าที่ต่ำกว่าจึงเก่ากว่าเสมอ พอหน้าต่างหมุน `resets_at` เปลี่ยน
  ค่าที่ลดลงกลายเป็นค่าที่ถูก · คีย์ของเจ้าของร่วม (`PROFILE_NAME`, `COST_*`) ต้องรอด
- **ไม่มี TTL** — เปอร์เซ็นต์ขยับได้ก็ต่อเมื่อผู้ใช้เรียก Claude ค่าเก่าจึง *คือ* ค่าที่ถูก
  สิ่งเดียวที่ทำให้หมดอายุคือ `resets_at` ที่ผ่านไปแล้ว
- **ลิงก์หลุด = พื้นที่ล่างว่างทั้งแถบ ทั้งการ์ดและโควตาหาย เหลือนาฬิกา**
  (`shown_card_count()` / `usage_shown()` ใน `ct_ui.c` ↔ `Screen.shown_cards()` /
  `.shown_usage()` ใน `gen/screen.py`) **การกลับคำ** — เดิมทั้งคู่ยังอยู่ โดยอ้างว่า
  countdown เดินด้วยนาฬิกาบอร์ดเองจึงยังจริง แต่นั่นตอบแค่ครึ่งเดียว: countdown จริงก็จริง
  ส่วน*เปอร์เซ็นต์*ซึ่งเป็นตัวเลขที่คนอ่านมาจาก host และไม่มีใครยืนยันแล้วว่ายังจริง —
  ผู้ใช้อาจสั่งงาน Claude จากเครื่องอื่น หรือแอปฝั่ง Mac ตายไปนานแล้ว "ไม่ขยับ" กับ
  "ขยับแต่บอร์ดไม่รู้" แยกกันไม่ออกบนจอ · ตอนหลุด สิ่งเดียวที่จอยืนยันได้คือเวลาที่ค้างอยู่
  (ซึ่งหรี่เป็นเทาเพื่อบอกว่าเป็นเวลาล่าสุดที่รู้ ไม่ใช่ตอนนี้) จอจึงเทาทั้งใบตามที่ README โฆษณา
  ไม่ใช่เทาครึ่งใบโดยมีตัวเลขสีสดที่เชื่อไม่ได้ค้างอยู่ท่อนล่าง
- **ทางเข้าที่สอง ไม่ใช่ตัวเขียน cache ตัวที่สอง** — payload จาก API (รูปแบบดูที่
  `docs/claude-usage-api.md`) เข้าทาง `UsageWriter.ingestAPI` ซึ่งอยู่ใน enum เดียวกับ
  ทางเข้าของ statusline และจบที่ `merge`/`write` ชุดเดียวกัน กฎ "เพิ่มอย่างเดียว"
  ต้องมีเจ้าของคนเดียว · กับดักที่เทสต์ปักไว้: `utilization` เป็น float แต่ `percent` เป็น int
  จึง **ปัด** ทั้งคู่ ไม่ตัดทิ้ง (16.6 ไม่งั้นได้ 16 ทางหนึ่ง 17 อีกทางหนึ่ง) · เวลาต้องผ่าน
  formatter ตัวเดิมก่อนเขียนเสมอ เพราะ `…:00Z` กับ `…:00.482Z` คือหน้าต่างเดียวกัน
  แต่คนละสตริง แล้ว merge จะนึกว่าหน้าต่างหมุน · เปอร์เซ็นต์ที่ไม่มี `resets_at` ที่อ่านออก
  ถูกทิ้งทั้งบาน · `weekly_scoped` เป็น limit ราย model ไม่ใช่ของทั้งบัญชี
- **เขียนไฟล์เดียวกับ Claude Usage.app** คีย์ชุดเดิม เวลา ISO8601 — สคริปต์ statusline
  ที่ผู้ใช้จูนไว้อ่านต่อได้ · เขียนแบบ temp + rename ไม่มีใครอ่านเจอไฟล์ครึ่งๆ
- **ยึดช่อง `statusLine.command` แต่ไม่เปลี่ยนสิ่งที่ผู้ใช้เห็น** — สคริปต์เขียน cache แล้วส่ง
  JSON ก้อนเดิมต่อให้คำสั่งเดิมแล้วพิมพ์ผลออกไป · ทุกทางจบด้วย `exit 0` · ติดตั้งซ้ำต้อง
  กู้คำสั่งเดิมจาก `PREV=` ในตัวสคริปต์ ไม่งั้นการอัปเกรด = ลบ statusline ของเขาทิ้งเงียบๆ
  · `refreshInterval: 10`
- **ไม่มีคำสั่งเดิมให้ส่งต่อ = เราวาดเอง และวาดให้เต็ม** (`StatuslineRender`) — เดิมทางนี้พิมพ์
  แค่ `5h 12% · 7d 30%` ซึ่งทำให้คนที่ยังไม่เคยตั้ง statusline รู้สึกว่าการเปิด "Read quota
  from the statusline" คือการแลกช่องไปได้ของที่ด้อยกว่า · ที่ port มาคือ `statusline-command.sh` ของ
  Claude Usage.app **ทั้งใบ** (ไอคอน เกรเดียนต์ 10 ขั้น ขีด pace 6 ขั้น รูปแบบเวลา) เพราะ
  ครึ่งใบแปลว่ามีสองหน้าตาในโลกให้ผู้ใช้เทียบแล้วสงสัย · **อ่าน
  `~/.claude/statusline-config.txt` ไฟล์เดียวกับสคริปต์นั้น** ไม่สร้างไฟล์ตั้งค่าของตัวเอง —
  คนที่ย้ายมาจากสคริปต์นั้นตั้งค่าไว้แล้ว และไฟล์ที่สองแปลว่าต้องตั้งสองที่ให้ตรงกันตลอดไป ·
  เทสต์ปักไว้ว่าทุกชิ้นและทุกโหมดสีต้องได้ผลเท่าสคริปต์ต้นทาง ต่างได้อย่างเดียวคือ escape
  ปิดสีที่โหมดขาวดำของสคริปต์นั้นพ่นทิ้งไว้เปล่าๆ · ตัวเลขที่เก่ากว่า 5 นาทีอ่านว่า `~` ตามเดิม
  (เราไม่มีทางยิงเน็ตแทนตรงนี้ — ไม่ถือ credential) · `git` ถูกเรียกจริงเพื่อหา branch และ
  จำนวนบรรทัด ไม่ใช่อ่าน `cost.total_lines_*` จาก payload: payload นับสิ่งที่ *session นี้* แก้
  แต่บรรทัดสถานะควรบอกว่า *ต้นไม้ทำงาน* ต่างจาก HEAD แค่ไหน · ทุกคำสั่งใส่
  `--no-optional-locks` เพราะมันวิ่งทุก 10 วินาทีระหว่างที่ผู้ใช้กำลังพิมพ์คำสั่ง git อยู่

**ท่อ poll**

- **ตัวยิงเป็นโปรเซสอายุสั้น ไม่ใช่ daemon** — อ่าน key → หา org → ยิง → `ingestAPI` →
  พิมพ์บรรทัดสรุป → `exit` · ไม่ต้องมี supervision ไม่มี backoff ไม่มี crash loop
  ไม่มีลูกกำพร้า และ credential อยู่ใน RAM แค่ช่วงยิง · เหตุผลที่ *ไม่* เอาไปไว้ใน daemon:
  daemon เปิดค้างทั้งวันเพื่อคุย BLE ถ้ามันถือ key ด้วย credential จะอยู่ในโปรเซสที่อายุ
  ยืนที่สุดในเครื่องตลอดเวลา ทั้งที่ใช้จริงวินาทีเดียวต่อนาที และวันที่ key หมดอายุ
  วิธีแก้จะกลายเป็นการรีสตาร์ทตัวที่ต่อบอร์ดอยู่
- **`sessionKey` คือ credential เต็มบัญชี** — ไฟล์ `~/.tamaclaude/session-key` mode 600
  เท่านั้น (group/other อ่านได้ = ปฏิเสธพร้อมบอกวิธีแก้) · ไม่เคยผ่าน argv (`ps` เห็น)
  ไม่เคยผ่าน env · **อ่านใหม่ทุกรอบ ไม่ cache** เพราะวิธีแก้ต้องเป็นแค่การ paste ทับไฟล์
  · ห้าม log request/header ทุกกรณี ข้อความ error สร้างจาก status code ล้วนๆ
  · org id ถูก validate (ปฏิเสธ `/` และ `..`) ก่อนต่อเข้า URL เสมอ แม้มาจาก response
  ของเราเอง — ปลายทางเป็นของคนอื่น
- **แอปเป็นคนเขียนไฟล์ key** — ช่องกรอกแบบปิดบัง แล้วสร้าง temp ด้วย 600 แล้ว rename
  ไม่ใช่เขียนก่อน `chmod` ทีหลัง (ช่วงระหว่างสองคำสั่งคือช่วงที่ credential เปิด)
  · ต้องลบไฟล์เดิมก่อน rename เพราะ `replaceItemAt` คัดลอกสิทธิ์ของไฟล์เดิมกลับมา
- **exit code แยกชนิดความล้มเหลว** — `2` key ถูกปฏิเสธ (401/403) · `3` ไฟล์ key ใช้ไม่ได้ ·
  `1` อย่างอื่น (5xx, เน็ตหลุด, timeout) · ล้มเหลวแบบไหนก็ **ไม่ลบไม่แตะ cache เดิม**
- **หยุดยิงเมื่อ key พัง ไม่หยุดเมื่อเน็ตพัง** — exit `2` เป็นอันเดียวที่ *ล็อก* (ยิงต่อก็ได้ 401
  เหมือนเดิมทุกนาที) ตั้ง key ใหม่แล้วปลดล็อกและยิงทันที · exit `3` เป็นแค่ป้ายบอกอาการ
  ตัวที่กันคือการอ่านสภาพไฟล์จริงทุกรอบ ผู้ใช้ที่ `chmod 600` เองข้างนอกจึงกลับมายิงได้
  โดยไม่ต้องเปิดปิดแอป — ล็อกที่ปลดได้ทางเดียวคือกับดัก · เครื่องตื่นจาก sleep ยิงทันที
  หนึ่งรอบ ไม่รอครบรอบถัดไป
- **ตัวจับเวลาอยู่ที่ menu bar ตัวเดียว** — `UsagePoller` ถูกป้อน `tick(now:)` จากนาฬิกา
  วินาทีละครั้งที่มีอยู่แล้ว ไม่มี timer ตัวที่สอง ตรรกะทั้งอันจึงเป็นฟังก์ชันของเวลาที่ส่งเข้ามา
  เทสต์ครบทุกกิ่งได้โดยไม่ต้องรอ · ลูกทีละตัว ลูกที่ไม่จบใน **30 วินาที** ถูกฆ่า (TERM แล้ว
  KILL) · ปิดแอป = ฆ่าลูกก่อนตาย · ลูกที่ถูกฆ่ายังพูดทีหลังได้ จึงมีเลขรุ่นกำกับ
- **รอบสามค่า: `Off` / `60s` (ตั้งต้น) / `5 min`** — หน้าต่าง 5 ชม. ขยับราวหนึ่งเปอร์เซ็นต์
  ต่อสามนาที ถี่กว่า 60 วิ คือการยิงเพิ่มเพื่อได้ค่าเดิม · `Off` เป็นค่าที่ผู้ใช้เลือกได้จริง
  จึงต้องแยกจาก "ยังไม่เคยเลือก" ไม่งั้นแอปที่เพิ่งติดตั้งจะกลายเป็น `Off` เงียบๆ
- **สถานะจากลูกมาทาง stdout ทางเดียว ไม่มีไฟล์สถานะตัวที่สอง** — รวมถึงตอนล้มเหลว
  และรวมถึงรายการ org (บรรทัด `org <id> <ชื่อ>` นำหน้าบรรทัดสรุป) ลูกตายทุกรอบ
  อะไรที่มันรู้จึงต้องพูดตอนยังมีชีวิต · รายการ org ไปกับความล้มเหลวด้วย (`Failure.orgs`)
  ไม่งั้นการพินไว้ที่ org ที่หายไปจะล็อกตัวเอง · id จาก stdout ยัง validate ซ้ำ
- **พินคือพินในลูก การถอยเป็นการตัดสินใจของพ่อ** — `TAMACLAUDE_ORG_ID` ถูกใช้ตามนั้นเสมอ
  ไม่ถูกแทนเงียบๆ ด้วยตัวแรก · การถอยไปตัวแรกเมื่อตัวที่เลือกหายจากบัญชีอยู่ที่ menu bar
  ซึ่งเห็นรายการทั้งอัน · org เดียว = ซ่อนเมนูทั้งอัน · ติ๊กอ่านจากตัวที่ยิงจริง ไม่ใช่ค่าที่จำไว้

**การเริ่ม session เอง**

**การกลับคำ** — เดิม host *อ่าน* อย่างเดียว: `--usage-poll` ไปหยิบตัวเลขมา และ daemon
ไม่เคยใช้โควตาแทนผู้ใช้เลยสักหน่วย สวิตช์ **Auto-start a session when idle** กลับคำข้อนั้น
เพราะสิ่งที่ผู้ใช้ซื้อไปคืออุปกรณ์ที่บอกสถานะได้ แต่พอไม่มีหน้าต่าง 5 ชม. เปิดอยู่ (เช้าวันใหม่
หรือปล่อยเครื่องทิ้งไว้เกินห้าชั่วโมง) ก็ไม่มีอะไรให้อ่าน — ปลายทางไม่มีตัวเลข ไม่ใช่ท่อพัง
ทางเดียวที่จะมีตัวเลขคือมี session และวิธีเดียวที่ผู้ใช้ทำได้คือเปิดเทอร์มินัลเอง ซึ่งกลับหัว
กลับหางกับเหตุผลที่มีจอนี้อยู่บนโต๊ะ

**ปิดโดยปริยาย** — การอ่านโควตาไม่มีราคา การเปิด session มี แอปที่อัปเกรดแล้วเริ่มคุยกับ
Claude เองเงียบๆ คือการใช้เงินของผู้ใช้โดยไม่ได้ขอ ด้วยเหตุผลเดียวกันนี้ ราคาต่อครั้งถูกกด
ให้ต่ำที่สุด: `-p` one-shot, model ถูกที่สุด (หน้าต่าง 5 ชม. นับรวมทุกโมเดล มีแต่
`weekly_scoped` ที่แยกตามโมเดล) และปิด MCP ทั้งหมด ซึ่งเป็นก้อนที่ใหญ่ที่สุดของค่าเปิด
session · **hooks ไม่ปิด** — เป็นทางเดียวที่ daemon จะรู้ว่ามี session อยู่ และการที่มาสคอต
ขยับคือครึ่งหนึ่งของเหตุผลที่ฟีเจอร์นี้มีอยู่

**รั้วสองชั้น ไม่ใช่ชั้นเดียว** — การเย็นตัวห้านาทีหลังลูกจบ *และ* หนึ่งครั้งต่อหนึ่งหน้าต่าง
อย่างแรกอย่างเดียวยังลองใหม่ชั่วนิรันดร์เมื่อ session เริ่มไม่ขึ้นจริง อย่างหลังอย่างเดียวยังยิงรัว
ได้ในช่วงก่อนที่รอบ poll จะรายงานหน้าต่างใหม่ (นานได้ถึงหนึ่งรอบเต็ม) · "หนึ่งครั้งต่อหน้าต่าง"
เขียนเป็น state machine (ยิงแล้ว → ต้องเคยเห็นหน้าต่าง → หน้าต่างหายไป → ยิงได้อีก)
ไม่ใช่การเทียบ `resets_at` ข้าม seam ซึ่งจะเป็นกฎสำเนาที่สองของ "มีหน้าต่างไหม" —
กฎนั้นมีเจ้าของแล้วคือ `MenuBadge.from(usage) == nil` และตัวยิงรับ `[UsageSnap]?`
ก้อนเดียวกับที่แบดจ์กินด้วยเหตุผลนี้

**ล็อกเฉพาะสิ่งที่คนเท่านั้นที่แก้ได้** — เส้นแบ่งเดียวกับที่ `PollBlock` ใช้กับ session key:
หา `claude` ไม่เจอ กับ ลูกที่จบเพราะยังไม่ได้ login คือสองอย่างที่ยิงอีกกี่รอบก็ได้ผลเดิม
จึงหยุดยิงและขึ้นบรรทัดในท้าย popover ส่วนเน็ตสะดุด/timeout/แยกไม่ออก ไม่หยุดอะไร ·
สิ่งที่แยกสองกลุ่มนี้ออกจากกันคือ*ข้อความ*ที่ลูกพ่นตอนตาย ไม่ใช่ exit code — `claude` ใช้ 1
กับทุกอย่างที่ผิดพลาด รายการ marker จึงต้องแคบไว้ก่อน (marker ที่กว้างเกินเปลี่ยนความ
ล้มเหลวชั่วคราวให้เป็นล็อกถาวรที่ผู้ใช้ต้องมาปลดเอง ส่วนที่แคบไปแค่ยิงซ้ำอีกไม่กี่รอบ) ·
รอบที่ไม่สำเร็จคืนกติกา "หนึ่งครั้งต่อหน้าต่าง" กลับเป็น *ยิงได้* ด้วย เพราะรอบที่ล้มไม่ได้เปิด
หน้าต่างอะไรไว้ให้รอ — การปล่อยให้ค้างคือการให้ความล้มเหลวชั่วคราวหนึ่งครั้งฆ่าฟีเจอร์นี้
ถาวรพอๆ กับล็อก แต่เงียบกว่า · ปิดสวิตช์แล้วเปิดใหม่คือคำสั่ง "แก้แล้ว ลองอีกที" ที่ล้างทั้ง
ล็อก การเย็นตัว และกติกาหนึ่งครั้งต่อหน้าต่าง ไม่ใช่แค่ล็อก (ล็อกที่ปลดแล้วแต่ยังรอหน้าต่าง
ที่จะไม่มีวันมา คือล็อกตัวที่สองที่ปลดไม่ได้)

**`claudePath` ไม่มี UI โดยตั้งใจ** — `defaults write com.tamaclaude.daemon claudePath <path>`
*แทนที่* รายการที่ไล่หาทั้งอัน ไม่ใช่ถูกเติมต่อท้าย คนที่ต้องใช้มันคือคนที่ติดตั้งไว้นอกสี่ที่ที่รู้จัก
ซึ่งพบน้อยเกินกว่าจะกินที่ถาวรในเมนูที่คนอื่นทั้งหมดต้องอ่านผ่าน · ด้วยเหตุผลเดียวกัน path
ที่ไล่หามาแล้วอยู่ใน tooltip ของบรรทัดในแผง ไม่ใช่ในตัวบรรทัด

**บนสาย BLE**

- **ส่ง "วินาทีที่เหลือ" ไม่ใช่เวลารีเซ็ต** — `"u":[[35,10800],[48,111600]]` บอร์ดนับถอยเอง
  countdown จึงเดินถูกตอน BLE หลุด และ daemon ไม่ต้องยิงใหม่ทุกนาที · ปัดลงเป็นนาที
  ไม่งั้น snapshot ต่างกันทุกวินาทีแล้วบอร์ดโดนยิงวินาทีละครั้ง
- **ขีด pace มาฟรี** — ความยาวหน้าต่างเป็นค่าคงที่ (18000 / 604800 วิ) บอร์ดหา
  `elapsed = window - remaining` เองได้ · ขีดอยู่ขวาของเนื้อแถบ = ใช้ช้ากว่าเวลา ·
  ซ้าย = เร็วเกินไป — 35% ไม่มีความหมายจนกว่าจะรู้ว่าผ่านไปกี่เปอร์เซ็นต์ของหน้าต่าง

**สีและเกณฑ์**

- **สามขั้นบนจอ**: <60 เขียว · 60–85 เหลือง · >85 แดง · **แซง pace = แดงทันที**
  (`pct > elapsed/window`) เพราะ "60% ตอนเหลือเวลาอีกครึ่ง" เป็นปัญหาคนละแบบกับ
  "60% ตอนหมดเวลาพอดี" — สีบอก *ว่ามีปัญหา* ขีดบอก *ว่าห่างแค่ไหน* ทั้งคู่จึงต้องอยู่
- **ไม่มี card ไม่มี LED จากโควตา** — card จะไปบังแผงโควตาเอง (ย้อนแย้ง) และ LED
  ที่แปลว่า "Claude ต้องการคุณ" อยู่แล้วจะมีสองความหมายจนแยกไม่ออก
- **ป้าย pill สีคงที่ต่อหน้าต่าง** — Current ส้ม (clay) · Weekly เขียว (good)
  สีไม่ตามระดับการใช้: ถ้าทั้งแถวเป็นสีเดียวตอนวิกฤต สีจะหยุดเป็นสัญญาณ ·
  รางใช้ `gray_dark` ไม่ใช่ `bg_slot` ซึ่งกลืนพื้นจอ

## ฝั่ง Mac (Swift)

ตารางไฟล์กับหน้าที่อยู่ใน `CLAUDE.md` — ที่นี่เก็บเฉพาะเหตุผล

**snapshot** — คีย์สั้นเพราะต้องพอดี 500 ไบต์ (MTU 517 หัก header แล้วเผื่อ)
```json
{"c":"14:32","d":"Mon 27 Jul","o":0,
 "s":[{"p":"tamaclaude","s":"building"}],
 "n":[{"t":"tamaclaude","b":"allow Bash?","k":"alert"}]}
```

**GATT UUID** — firmware ต้องใช้ชุดเดียวกัน
```
service  7A9B0001-4C1E-4B6D-9E2A-1D5C3F0A0001
state    ...0002   daemon เขียน snapshot ลงตัวนี้ (write with response)
config   ...0003   ความสว่าง ฯลฯ
event    ...0004   บอร์ดแจ้งกลับ — สงวนไว้ v2
```

**invariant ของ daemon**

- **เกินโควตาแล้วหด `n`** (body ก่อน แล้ว title แล้วตัดใบล่างทิ้ง) แต่ไม่เคยตัด `s`
  — session คือสิ่งที่จอนี้มีไว้แสดง
- **encode ด้วย `sortedKeys` เสมอ** ไม่งั้นการเทียบว่า "เปลี่ยนไหม" จริงทุกครั้ง
  แล้วบอร์ดโดนยิงทุกวินาที
- **ทุกท่าอยู่บนจออย่างน้อย 5 วิ (`Timings.minPose`)** — Read/Edit จบใน ~100 มิลลิวินาที
  ถ้าเปลี่ยนท่าทันที ท่านั้นสั้นกว่าหนึ่งเฟรมของบอร์ด (tick 1 วิ + ดีเลย์ BLE) ผู้ใช้จะเห็นแต่
  `thinking` · ท่าที่ถูกข้ามหายไปเลย **ไม่เข้าคิว** — คิวทำให้มาสคอตเล่าอดีตช้ากว่าความจริง
  เรื่อยๆ เมื่อเครื่องมือยิงรัว · ข้อยกเว้น: priority สูงกว่า (error/waiting) แทรกได้ทันที
  และ entering/leaving ไม่ถูกหน่วงเพราะมีนาฬิกาของตัวเอง
- **นอนชนะการทวงถาม** — Stop แล้วเงียบเกิน 45 วิ ขึ้น `waiting` แต่ครบ 5 นาทีเปลี่ยนเป็นนอน
  ท่ายืนทวงตลอดกาลไม่สื่ออะไรเพิ่ม · การเตือนยังอยู่ในรูปการ์ดซึ่งเป็นคนละช่องทาง
- **เกิน 3 ตัว: เลือกด้วยความสำคัญ แต่วาดตามลำดับเกิด** — ตัวที่ตกจอคือตัวที่เงียบสุด
  และเก่าสุด ลำดับซ้าย→ขวาของตัวที่เหลือไม่เคยสลับ
- **session หายจากจอเมื่อ process ของมันตาย ไม่ใช่เมื่อ `SessionEnd` มา** — ปิดหน้าต่าง
  terminal ทิ้งคือทางออกที่ผู้ใช้ใช้จริงที่สุด และทางนั้น Claude Code โดน SIGHUP ก่อนได้ยิง
  hook ผลคือ session ผีค้างจอจนครบ `evict` (1 ชม.) แล้วไปกิน slot ของตัวเป็น · `--hook`
  จึงไต่สายบรรพบุรุษของตัวเองหา process ของ Claude Code แล้วแนบมากับทุกเหตุการณ์ ให้
  `prune()` เป็นคนถาม kernel ว่ายังอยู่ไหม (`ProcessTree`) — `evict` เหลือไว้เป็นตาข่ายรอง
  - **ไต่หาไม่เจอ = "ไม่รู้" ไม่ใช่ "ตายแล้ว"** hook ถูกเรียกผ่านเชลล์และผู้ใช้ครอบ wrapper
    อะไรไว้ก็ได้ ถ้าเดาผิดไปคว้า wrapper ที่ตายทันทีหลัง hook จบ session ที่ยังทำงานอยู่จะ
    หายจากจอทุกครั้ง — ผิดทางนี้แก้ไม่ได้ด้วยการรอ ต่างจากการค้างนานเกินไป
  - **ตัวระบุตัวตนคือคู่ (pid, เวลาเกิด)** PID ถูกใช้ซ้ำได้ในไม่กี่นาทีบนเครื่องที่ spawn
    process ถี่ (ซึ่ง Bash tool ทำอยู่ตลอด) เทียบแค่เลขแล้ว session ที่ตายไปจะฟื้นขึ้นจอ
  - **ห้ามระบุ Claude Code ด้วยชื่อ process** ตัวติดตั้งหลักวาง executable ไว้ที่
    `~/.local/share/claude/versions/<version>` ชื่อไบนารีจึงเป็นเลขเวอร์ชันและเปลี่ยนทุก
    การอัปเดต · ที่นิ่งคือคอมโพเนนต์ `claude` ในพาธ และต้องเทียบทั้งคอมโพเนนต์ ไม่ใช่
    `contains` เพราะไบนารีของเราเองอยู่ใน `tamaclaude/`
- **hook ต้องคืน 0 เสมอ** แม้ daemon ไม่ทำงาน — hook ที่พังทำให้ session ผู้ใช้พังตาม
  ซึ่งแย่กว่าจอไม่ขยับมาก
- **ไม่ใช้ `testTarget`** — เครื่องที่มีแค่ Command Line Tools จะ build ชุดเทสต์ผ่าน
  แต่ไม่รันมันเลยและยัง exit 0 ซึ่งอันตรายกว่าไม่มีเทสต์

**การแจกจ่ายและสิทธิ์**

- **daemon ต้องรันจาก `.app` bundle จริง** — TCC บน macOS 26 ไม่ยอมรับ `__info_plist`
  ที่ฝังในไบนารี Mach-O เปล่า ผลคือ CoreBluetooth เงียบสนิท (ไม่มี callback ไม่มี error)
  และพอเรียกจาก Terminal ที่มีสิทธิ์ถามได้จริง มันยิง SIGABRT ทิ้งทั้งโปรเซส
  (`__TCC_CRASHING_DUE_TO_PRIVACY_VIOLATION__`)
- **ลายเซ็นเป็น adhoc** — สิทธิ์ TCC ผูกกับ cdhash บิลด์ใหม่ = ถามสิทธิ์ Bluetooth ใหม่
  การแจกให้เครื่องอื่นต้องมี Developer ID + notarization
- **บันเดิลชื่อ `TamaClaude.app` ส่วนไบนารีข้างในยังชื่อ `tamaclaude`** — ชื่อบันเดิลคือ
  สิ่งที่ผู้ใช้เห็นใน Finder ส่วนชื่อไบนารีคือสิ่งที่ผู้ใช้พิมพ์ · ที่ห้ามเปลี่ยนตามคือ
  bundle id (`com.tamaclaude.daemon`), `~/.tamaclaude`, และชื่อ BLE ที่เฟิร์มแวร์ประกาศ
  (`BLETransport.deviceName`) — สองอันหลังเป็นสัญญากับของที่อยู่นอกแอป
- **การเปลี่ยนชื่อบันเดิลทำให้ hook กับ statusline ตายเงียบ** — ทั้งคู่เก็บ *พาธเต็ม*
  ของไบนารีลง `settings.json` ตอนกดติดตั้ง ไม่ได้ค้นหาแอปตอนรัน · ไม่ทำ self-heal เพราะ
  แอปที่ไปเขียนทับ `settings.json` เองตอนเปิดโดยไม่ได้ถูกสั่งคือแอปที่แก้ไฟล์ของผู้ใช้
  ตามใจตัวเอง · `make-app.sh --install` จึงลบ `/Applications/tamaclaude.app` ตัวเก่าทิ้ง
  (สองบันเดิลที่รันได้จะแย่ง socket กัน) แล้วบอกให้กดติดตั้งซ้ำหนึ่งครั้ง
- **เมนูบาร์ *เป็น* daemon เอง** ไม่ใช่ตัวสั่ง daemon อีกตัว — สิทธิ์ Bluetooth ผูกกับ `.app`
  ที่ปล่อยผ่าน LaunchServices แยกโปรเซสเมื่อไรได้ daemon ที่ต่อบอร์ดไม่ได้ทันที
- **`-v` และ `-psn_...` ต้องไม่ถูกอ่านเป็นโหมด** — LaunchServices แถม `-psn_0_12345`
  แอปที่ปฏิเสธอาร์กิวเมนต์ที่ไม่รู้จักจะตายทันทีที่ดับเบิลคลิก
- **เลือกบอร์ดจากเมนูของแอป ไม่ใช่หน้า Bluetooth ของ macOS** — BLE peripheral ที่เป็น
  GATT ล้วนไม่โผล่ในหน้าตั้งค่าเลย ระบบเปิดทางให้เฉพาะแอปที่สแกนเองผ่าน CoreBluetooth
  · บอร์ดโฆษณาชื่อ `tamaclaude-<MAC 2 ไบต์ท้าย>` เพื่อแยกหลายบอร์ดในห้องเดียวกัน
  · daemon สแกนต่อแม้ต่อบอร์ดได้แล้ว ไม่งั้นสลับไปตัวอื่นไม่ได้จนกว่าจะถอดปลั๊ก

**ไอคอนแถบเมนู**

- **คือแถบโควตา 5 ชม. ไม่ใช่โลโก้** — โลโก้กินพื้นที่ถาวรเพื่อบอกแค่ว่า "แอปยังรันอยู่"
  · แถบ 35x11 + `42%` ตัวเลขความกว้างคงที่ (ไม่งั้นไอคอนขยับทุกครั้งที่เปลี่ยนหลัก)
- **มีขีด pace ด้วย ไม่ใช่มีแต่สี** — `MenuBadge` เก็บ `pace` เป็น *ตำแหน่ง* ไม่ใช่ bool
  (สีคำนวณกลับจากตำแหน่งได้ แต่ตำแหน่งคำนวณกลับจากสีไม่ได้)
- **สองสถานะ ไม่ใช่สามขั้น** — ปกติเป็น template ขาวดำ (ระบบกลับสีให้) · ตอนเตือนเลิกเป็น
  template แล้ววาดแดงตรงๆ · ไอคอน 16 px ที่เหลือบครึ่งวินาทีมีคำถามเดียวคือ
  "ต้องช้าลงไหม" จึงใช้ **แซง pace เป็นเกณฑ์เดียว ไม่มีเกณฑ์เปอร์เซ็นต์ตายตัว** —
  90% ตอนเหลือเวลาอีกนิดเดียวคือโควตาที่ใช้ตามแผนพอดี สีแดงที่ขึ้นทุกครั้งที่ใกล้หมด
  หน้าต่างจะหยุดเป็นสัญญาณ · ไม่รู้ countdown = เทียบ pace ไม่ได้ = ไม่เตือน
- **ตอนเตือน แดงเฉพาะ *ขีด pace*** — สิ่งที่ผิดปกติคือความสัมพันธ์ระหว่างเนื้อแถบกับขีด
  ภาพที่แดงทั้งอันไม่ชี้ว่าให้มองตรงไหน แถมกลืนตัวเลข · หมึกที่เหลือต้องเลือกเองจาก
  appearance ของ *ปุ่ม* ไม่ใช่ของแอป (แถบเมนูมืดได้ทั้งที่แอปสว่าง) และธีมต้องเป็น
  ส่วนหนึ่งของกุญแจ "วาดไปแล้วหรือยัง" ไม่งั้นสลับธีมแล้วหมึกค้าง
- **ขีดเจาะเป็นร่องโปร่งตรงที่พาดผ่านเนื้อแถบ** (หมึกทับหมึกมองไม่เห็น) แต่วาดด้วยหมึก
  ตรงที่อยู่บนรางว่าง · เส้นขอบวาดท้ายสุด ไม่งั้นร่องทำให้กรอบขาดเป็นสองท่อน
  · บนจอบอร์ดใช้วิธีล้นขอบแทน เพราะที่นั่นวาดทับพื้นทึบ ไม่มี alpha ให้เจาะ
- **แถบมีเส้นขอบทึบ** — พื้นหลังแถบเมนูคือวอลเปเปอร์ของผู้ใช้ รางที่จางพอจะไม่แย่งสายตา
  ก็จางพอจะหายไปกับภาพข้างหลัง · เนื้อแถบเว้นจากขอบ 1.5 px ทั้งสี่ด้าน
- **มุมมน 2.5 ไม่ใช่ครึ่งความสูง** — pill อ่านเหมือนปุ่ม ส่วนนี่คือเครื่องวัด และปลายที่โค้ง
  จนกลมทำให้ 3% แรกกับสุดท้ายกินพื้นที่ไม่เท่ากับ 3% ตรงกลาง
- **ตอนเมนูเปิด แบดจ์กลับไปเป็น template แม้กำลังเตือน** — ระบบถมพื้นปุ่มด้วยสีเน้น
  แดงบนน้ำเงินอ่านไม่ออก และนั่นคือสถานะที่ผู้ใช้มองนานที่สุดตอนมันแดง (เขากดเพราะมันแดง)
- **ไม่รู้โควตา → เครื่องวัด (`gauge...50percent`, template) ไม่ใช่ `0%` ไม่ใช่แถบเปล่า** —
  `0%` ที่เดาเอาคือคำโกหกที่ผู้ใช้เชื่อทันที ส่วนแถบเปล่าดูเหมือนแอปพัง · countdown
  ถึงศูนย์นับเป็น "ไม่รู้" ด้วย · ที่ตรงนี้จะกลายเป็นแถบโควตาทันทีที่รู้ตัวเลข ไอคอนแทน
  จึงเป็นเครื่องมือชนิดเดียวกันที่เข็มยังไม่ขยับ ไม่ใช่ `desktopcomputer` ที่พูดถึงจอ
  **กลับคำ**: เคยใช้ `NSApplication.shared.applicationIconImage` ย่อลง ด้วยเหตุผลว่าตรา
  ของแอปพูด "แอปอยู่ แต่ยังไม่รู้ตัวเลข" ตรงกว่าสัญลักษณ์ที่ยืมความหมายมา — แต่โลโก้
  มีแสงเงาและรายละเอียด ที่ 16 px เหลือก้อนส้มที่บอกอะไรไม่ได้ แถมไม่ใช่ template
  จึงไม่กลับสีตามธีม สิ่งที่ผู้ใช้เห็นจริงชนะเหตุผลเรื่องความหมาย
- **ไม่หรี่ไอคอนตอนต่อบอร์ดไม่ติด** — โควตาไม่ได้มาจากบอร์ด การหรี่ทำให้ข้อมูลที่ยังถูก
  ดูเหมือนใช้ไม่ได้ · สถานะบอร์ดอยู่ที่ท้าย popover

## แผง popover

- **คลิกไอคอนได้ popover ไม่ใช่เมนู · เฟืองเหลือแค่ "Settings…" กับ "Quit"** — แผงมีแต่
  ของที่ต้อง *มอง* ของที่ต้อง *ตั้งค่า* อยู่ในหน้าต่างตั้งค่า
  · **กลับคำ**: เดิมเฟืองเด้ง `NSMenu` เต็มใบ เพราะติ๊กถูก/submenu/slider ได้มาฟรี
  แต่หน้า Wi-Fi ต้องมีรายชื่อที่ยาวไม่แน่นอน ช่องรหัสผ่านที่ต้องพิมพ์ และสถานะที่ขยับเอง
  ระหว่างที่ผู้ใช้มองอยู่ — เมนูปิดตัวเองทุกครั้งที่คลิก ทั้งสามอย่างจึงอยู่ในเมนูไม่ได้
  เมื่อต้องมีหน้าต่างอยู่ดี การมีสวิตช์กระจายสองที่แย่กว่าการย้ายมาไว้ที่เดียว
  (`PreferencesWindowController`) · ของที่หายไปพร้อมเมนู: ความรู้เรื่องความกว้าง `NSMenu`
  และแถวสไลเดอร์ที่ต้องยืดเอง — ทั้งหมดเป็นราคาของเมนู ไม่ใช่ของฟีเจอร์
- **popover เป็น `.applicationDefined` แล้วปิดเองด้วยตัวดักคลิก** — `.transient` ปิดตัวเอง
  เมื่อได้เหตุการณ์นอกหน้าต่าง ซึ่งรวมถึงตอนเมนูเฟืองของมันเองกำลัง track อยู่
  แผงจึงหายไปใต้เมนูของตัวเอง
- **การ์ดใช้เกณฑ์สีสามขั้นของบอร์ด ไม่ใช่สองสถานะของแถบเมนู** — ที่นี่มีที่ให้ไล่สีและผู้ใช้
  กำลังมองอยู่จริง · ฝั่ง host **คัดลอก** `warn_pct`/`crit_pct` จาก `tools/layout.toml`
  ด้วยมือ — ตัวสร้าง TOML→Swift เพื่อค่าสองค่าคือเครื่องจักรที่ต้องดูแลมากกว่าค่าที่มันดูแล
  คอมเมนต์อ้างชื่อคีย์ต้นทางไว้แทน · สูตร pace อยู่ที่ `UsageReader.elapsedPercent`
  ที่เดียว เพราะแบดจ์กับการ์ดต้องได้ตัวเลขเดียวกันเสมอ
- **แต่ *สี* บนการ์ดเป็นสีระบบ ไม่ใช่พาเลตต์ของบอร์ด** — พาเลตต์ใน `layout.toml` จูนมา
  สำหรับ TFT ที่หรี่ไฟหลัง บนพื้นดำของ popover อ่านเป็นสีขุ่น · `systemGreen/Orange/Red`
  ตามธีมและการตั้งค่าการเข้าถึงให้เอง · สิ่งที่ต้องตรงกันคือ *เกณฑ์ที่เลือกสี* ไม่ใช่ค่า RGB
- **ชื่อการ์ดบรรทัดบน คำอธิบายบรรทัดล่าง weekly ใช้ pill แทนคำอธิบาย** — ชื่อกับคำอธิบาย
  ในบรรทัดเดียวกันแย่งกันเป็นสิ่งที่อ่านก่อน และบีบเปอร์เซ็นต์จนไม่ชิดขวาเวลาชื่อยาว
- **ขีด pace อยู่บนแถบเสมอ ไม่ใช่เฉพาะตอนแซง** · วาดด้วยสีข้อความทับตรงๆ ได้
  ไม่ต้องเจาะร่องอย่างบนแถบเมนู เพราะพื้นหลังเป็นพื้นทึบของแผง
- **เวลารีเซ็ตบอกทั้งสัมพัทธ์และสัมบูรณ์** — `Resets in 2h24m (Today 23:00)` อันแรกตอบ
  "อีกนานไหม" อันหลังตอบ "ตอนนั้นคือเมื่อไรของวัน" · weekly ไกลถึงเจ็ดวัน
  `Today/Tomorrow` จึงไม่พอ ต้องมีชื่อวันและวันที่
- **บรรทัดอายุของค่ามีหน่วยวินาที** ต่างจากที่อื่นในแอป — คำถามที่ทำให้ฟีเจอร์นี้เกิดคือ
  "เลขนี้ค้างหรือเปล่า" คำตอบระดับนาทีแค่ย้ายความสงสัยจากบอร์ดมาไว้บนเมนูบาร์
  และตัวเลขที่เดินทุกวินาทีเป็นหลักฐานว่าแผงยังมีชีวิต · อยู่ *ใต้การ์ด* ไม่ใช่ท้ายแผง
  เพราะเป็นคำอธิบายของตัวเลขที่อยู่เหนือมัน
- **สองบรรทัดที่พูดคนละเรื่อง** — "key หมดอายุ" (ต้องลงมือ กดที่บรรทัดเพื่อใส่ key ใหม่)
  กับ "ค่านี้อายุเท่าไร" ยุบเป็นบรรทัดเดียวเมื่อไร ผู้ใช้จะแยกไม่ออกว่า "เก่า" แปลว่าต้องทำ
  อะไรหรือไม่ต้องทำอะไร — ค่าเก่ายังถูกได้เสมอในโดเมนนี้
- **ไม่มีแถวสถานะระบบ** — แถวที่เขียนว่า "ปกติ" เกือบทุกวันจะกลายเป็นสิ่งที่สายตาข้าม
  พอถึงวันที่มันไม่ปกติก็ไม่มีใครอ่าน · สิ่งที่ต้องลงมือมีบรรทัดของตัวเองเฉพาะตอนที่ต้องลงมือ
  (`PanelText.keyProblem`) ส่วนสิ่งที่ตั้งค่าได้อยู่หลังเฟืองพร้อมเครื่องหมายถูก ซึ่ง *คือ*
  ความจริง ไม่ใช่สำเนา · บรรทัดอายุของค่าตอบ "ท่อยังเดินอยู่ไหม" ได้ตรงกว่าแถวที่บอกว่า
  ท่อ *ควร* จะเดิน · ข้อยกเว้นเดียวคือบรรทัดบอร์ด เพราะเป็นสถานะของอุปกรณ์คนละที่กับแอป
- **หน้าตั้งค่าอ่านสถานะจริงทุกครั้งที่เปิด ไม่ใช่จำไว้ตอนสร้างหน้าต่าง** — ติ๊กสองอัน
  (statusline, launch at login) แก้จากข้างนอกได้โดยแอปไม่รู้เรื่อง และหน้าต่างมีอายุยืน
  กว่าเมนูที่ถูกสร้างใหม่ทุกครั้ง · ตัว controller ไม่รู้จัก BLE ไฟล์ หรือ `UserDefaults`
  เลย ทุกอย่างเข้าออกผ่าน closure เพื่อให้ที่เก็บสถานะจริงยังมีที่เดียว
- **หน้า Wi-Fi ยกเรื่อง client isolation ขึ้นมาก่อนที่จะพัง** — เน็ตโรงแรม/ออฟฟิศให้บอร์ด
  ขึ้นเน็ตได้ตามปกติแล้วเงียบทีหลัง อาการนั้นอ่านเหมือนบั๊กของเรา ไม่ใช่ของเน็ต
- **ลิงก์โปรเจกต์อยู่ในหน้าตั้งค่า ไม่ใช่บนแผง** — เคยวางไว้ท้ายแผงแล้วผิดกฎข้อแรกของหมวดนี้:
  แผงมีแต่ของที่ต้อง *มอง* ลิงก์เป็นของที่ *กด* และไม่ใช่สถานะของอะไรเลย · ชื่อรายการ
  *คือ* ปลายทาง (`github.com/thaitop/tamaclaude` ไม่ใช่คำว่า "GitHub") — แอปนี้ขอ
  credential เต็มบัญชีจากผู้ใช้ ลิงก์ที่ซ่อนปลายทางไว้หลังคำสวยๆ เป็นท่าเดียวกับที่ผู้ใช้ควรระวัง
- **ไม่มีค่าเลยทั้งสองหน้าต่าง → ซ่อนการ์ดทั้งช่อง เหลือแต่บรรทัดอายุ** · หน้าต่างที่รู้ใบเดียว
  ยังโชว์ทั้งคู่ โดยใบที่ไม่รู้เขียน `--%` สีจาง ไม่ใช่ `0%` สีเขียว
- **ปุ่ม refresh เป็นทางออกฉุกเฉิน — ยิงหนึ่งรอบ แล้วเย็นตัว 10 วินาที** — endpoint นี้
  ไม่มีเอกสาร ปุ่มที่กดรัวได้จะทำลายเหตุผลที่เราตัดตัวเลือก 30 วินาทีทิ้ง · ทางปกติคือ
  *การเปิดแผง* เอง (`RefreshControl.wantsPoll`) ซึ่งเป็นสัญญาณความตั้งใจที่ชัดพอ ·
  เย็นตัวนับจากตอน *จบ* ไม่ใช่ตอนเริ่ม ไม่งั้นรอบที่ช้ากว่าสิบวินาทีจะไม่มีการเย็นตัวเลย ·
  เกณฑ์ตอนเปิดดูสองอย่าง: ค่าล่าสุดที่ *กลับมา* (`UsageReader.stamp`) กับรอบล่าสุดที่
  *ออกไป* (`UsagePoller.lastStarted`) — รอบที่ล้มเหลวไม่เคยขยับอันแรก ถ้าดูแต่อันแรก
  การเปิดปิดแผงตอนเน็ตล่มจะยิงลูกทุกครั้งที่ชำเลืองดู
- **`Off` และ key ที่หมดอายุไม่ปิดปุ่ม** — `Off` สั่งว่าแอปอย่ายิง *เอง* ไม่ได้ห้ามผู้ใช้ดูค่าใหม่
  · `UsagePoller` มีสองประตู: `pollNow` (นาฬิกา เคารพทุกกฎ) กับ `refreshNow` (คน ข้าม `Off`
  และข้ามล็อก) เหลือแค่ "ลูกวิ่งอยู่แล้ว" กับ "ไม่มีไฟล์ key" ที่ยังกันทั้งคู่
- **การเย็นตัวเป็นวินัยของ *ปุ่ม* ไม่ใช่ของ poller** — ถ้านับรอบอัตโนมัติด้วย ที่ 60 วินาที
  ปุ่มจะกดไม่ได้หนึ่งในหกของเวลาโดยไม่มีใครเข้าใจว่าทำไม · `MenuBarApp` จำเองว่ารอบไหน
  เป็นของปุ่ม และตั้งค่านั้นจาก `isRunning` ไม่ใช่ตั้ง `true` ดื้อๆ
- **กำลังยิง = ตัวหมุนแทนที่ปุ่ม ไม่ใช่ปุ่มที่จางลง** — ปุ่มจางอ่านได้ว่า "กดไม่ได้ตอนนี้"
  ซึ่งเป็นภาพเดียวกับตอนเย็นตัว · ทั้งคู่ยืนในกรอบขนาดคงที่ ไม่งั้นหัวแผงขยับทุกครั้ง
- **ตัวสลับ org อยู่หลังลูกศรข้างชื่อที่หัวแผง ไม่อยู่ในเมนูเฟือง** — ที่สลับสองที่แปลว่าผู้ใช้
  ต้องจำว่าอันไหนคืออันจริง · ลูกศรโผล่เมื่อมีมากกว่าหนึ่ง org เท่านั้น · ยังไม่ได้ตั้ง key =
  หัวแผงเป็นชื่อแอป ไม่ใช่ชื่อที่ค้างมาจาก key ตัวก่อน

**autolayout ของ popover** (สามข้อนี้แก้ปัญหาเดียวกันคนละด้าน)

- **`NSPopover` โตตาม autolayout ได้เอง แต่ไม่หดกลับ** — ต้องตั้ง `preferredContentSize`
  จาก `fittingSize` ทุกครั้งที่เนื้อในเปลี่ยน (ไม่ใช่ทุกวินาที — แผงวาดใหม่ทุกวินาที
  แต่ขนาดเปลี่ยนนานๆ ครั้ง) ไม่งั้นแถวที่หายไปกลายเป็นช่องว่างท้ายแผง
- **ขอบล่างของแผงและของการ์ดหักได้ (priority 999) ส่วนขอบบนเป็นข้อบังคับ** — ความสูงจริง
  ของเนื้อในเปลี่ยนได้ทีหลัง (การ์ดโผล่, ลูกศร org มาถึงหลัง poll แรก) ในจังหวะที่กรอบ
  ยังเตี้ยกว่าเนื้อใน · ถ้าขอบล่างเด็ดขาด สิ่งที่ยอมหักคือระยะห่างระหว่าง view — หัวแผง
  กับการ์ดใบแรกทับกัน · ปล่อยให้ขอบล่างหักแล้วส่วนเกินล้นไปโดนตัดที่ `root.clipsToBounds`
  ซึ่งอ่านว่า "ยังไม่นิ่ง" · `resize()` จัด layout ซ้ำทันทีในเฟรมเดียวกันหลังตั้งขนาด
- **ทุกบรรทัดต้านการบีบด้วย `required` และ stack ทุกชั้นไม่ยืดตามกรอบ** — 999 ยังชนะ
  แรงต้านการบีบตามค่าปริยายของ `NSTextField` (750) กรอบที่เตี้ยกว่าจึงกดแถวจนสูงเป็นศูนย์
  หัวการ์ดหายไปทั้งแถวโดยที่การ์ดยังดูปกติ · ทางกลับกัน กรอบที่สูงเกิน (popover ที่ยังไม่หด)
  ถูก `NSStackView` แจกส่วนเกินให้ arranged view ทั้งหมด การ์ดใบแรกจึงพองเป็นช่องว่าง ·
  แก้ที่เดียว: `compressionResistance` ของทุกบรรทัด + `hugging`/`clippingResistance`
  แนวตั้งของ stack ทุกชั้นเป็น `required`
- **แผงต้องถมไฮไลต์ให้ปุ่มบนแถบเมนูเอง** — การถมมาฟรีกับ `statusItem.menu` เท่านั้น
  และแบดจ์ต้องวาดใหม่ตอนถูกถม (ภาพที่ไม่ใช่ template อ่านไม่ออกบนพื้นสีเน้น)
- **แผงที่เปิดค้างมีนาฬิกาของตัวเองที่หยุดที่ `popoverDidClose` ที่เดียว** — อ่าน cache
  จากดิสก์ทุกวินาทีเพื่อข้อความที่ไม่มีใครมองคือเผาแบต · ทางปิดมีหลายทาง จึงรวมไว้จุดเดียว
- **ขอบการ์ดใช้ `tertiary` ไม่ใช่ `separatorColor`** — `separatorColor` จูนมาสำหรับพื้นสว่าง
  บนพื้นดำของ popover มันจางจนการ์ดสองใบอ่านเป็นข้อความสี่ก้อนที่ไม่รู้ว่าก้อนไหนคู่กัน
  · พื้นการ์ดช่วยไม่ได้เพราะจงใจจางเพื่อไม่ให้เป็นกล่องซ้อนกล่อง
- **รางของแถบในการ์ดใช้ `tertiary` ไม่ใช่ `quaternary`** — รางวาดบนพื้นการ์ดที่จางอยู่แล้ว
  อีกที ระดับจางที่สุดซ้อนบนของจางกลืนไปกับพื้น แล้วเนื้อแถบอ่านเป็นขีดลอยๆ ที่ไม่รู้ว่า
  ยาวเทียบกับอะไร · บนแถบเมนูไม่มีปัญหานี้เพราะมีเส้นขอบทึบบอกขอบเขตอยู่แล้ว

## ฝั่ง firmware (ESP-IDF v5.5)

```
firmware/main/ct_lcd.c      SPI + init ILI9341 ตามผลวัด + ไฟหลัง PWM (LEDC)
firmware/main/ct_rects.c    rect list ในพิกัด unit + ขอบ + กรอบรวม
firmware/main/ct_props.c    prop ทั้ง 9 ชิ้น  (พอร์ตตรงจาก tools/gen/props.py)
firmware/main/ct_mascot.c   mood + สถานะ + การจัดกึ่งกลาง (พอร์ตจาก mascot.py)
firmware/main/ct_model.c    JSON snapshot -> struct (cJSON)
firmware/main/ct_ui.c       หน้าจอทั้งใบด้วย LVGL 9.2 (โครงเดียวกับ screen.py)
firmware/main/ct_ble.c      NimBLE GATT server 3 characteristic + bonding
firmware/main/ct_wifi.c     WiFi station: สแกน จำเครือข่าย ต่อใหม่เอง (ดูหัวข้อ "WiFi")
firmware/main/ct_lan.c      TCP 7333 + AES-GCM + mDNS — ทางเดินที่สองของ snapshot
firmware/main/ct_led.c      RGB LED กะพริบตอนมีการเตือนใหม่
firmware/main/main.c        ประกอบทุกอย่าง + ลูปหลัก
```

`idf.py -p /dev/cu.usbserial-XX flash monitor` — บิลด์ได้ ~900KB ใน partition 3MB

- **มาสคอตวาดด้วย `LV_EVENT_DRAW_MAIN` ต่อ slot** ไม่ใช่ canvas — canvas เต็มจอกิน 150KB
  ซึ่งบอร์ดไม่มีให้ ส่วน draw callback วาดลงบัฟเฟอร์บางส่วนของ LVGL โดยตรง
- **ฉากท้องฟ้าเป็น object ผืนเดียวที่สร้างก่อนใครเพื่อน** — LVGL เรียงชั้นตามลำดับสร้าง
  · เมฆอยู่ในผืนเดียวกัน ไม่ใช่ object แยก: แถบฟ้า 320x98 น้อยกว่าที่ 3 slot วาดใหม่
  ทุกเฟรมอยู่แล้ว จึงไม่ต้องแลกความซับซ้อนกับอะไรเลย
- **ฟ้าวาดใหม่เมื่อภาพเปลี่ยนจริง ไม่ใช่ทุกเฟรม** — เมฆขยับ 4 px/วิ (4 ครั้ง/วิ) ส่วนลูปหลัก
  เดิน 16 เฟรม/วิ · `lv_obj_invalidate_area` เฉพาะแถบฟ้า พื้นดินกับหญ้าวาดใหม่เมื่อ
  ช่วงเวลาเปลี่ยนเท่านั้น
- **ตารางดาว/หญ้า/เมฆ generate เป็น C array จาก `layout.toml`** — ถ้าแต่ละฝั่งสุ่มเอง
  ภาพบน preview จะไม่ใช่ภาพบนบอร์ด แล้ว preview ก็หมดประโยชน์
- **เงาใต้เท้าวาดเป็นแคปซูล ไม่ใช่วงรี** — `lv_draw_rect` clamp radius ที่ครึ่งด้านสั้น
  ฝั่ง preview จึงต้องใช้ `rounded_rectangle` ตาม ไม่ใช่ `ellipse` ที่สวยกว่าแต่คนละรูป
- **ปิด `LV_USE_TEXTAREA` ไม่ได้** — spinbox ใน LVGL 9.2 พึ่งมันแบบ `#error`
- **`ct_model_parse` เขียนทับ snapshot เดิมทีเดียวตอนท้าย** — JSON พังกลางทางต้องไม่ทิ้ง
  ภาพครึ่งๆ
- **สถานะที่ firmware ไม่รู้จักตกเป็น `idle`** ไม่ใช่ค้างจอ — daemon อาจใหม่กว่า firmware
- **ชื่ออุปกรณ์อยู่ใน scan response ส่วน adv บรรจุ service UUID 128 บิต** — ทั้งคู่ไม่พอ
  ใน 31 ไบต์ และฝั่ง Mac สแกนด้วย UUID จึงต้องเป็นตัวที่อยู่ใน adv
- **`ct_lan.c` เปิด server ตามการมี IP ไม่ใช่ตามการมีกุญแจ** — บอร์ดที่รับสายแล้วปฏิเสธ
  ทุกเฟรมบอกฝั่ง Mac ได้ว่ากุญแจไม่ตรง ส่วนบอร์ดที่ไม่รับสายเลยแยกไม่ออกจากบอร์ดที่ตาย
- **สายใหม่ชนะสายเก่าเสมอ** — Mac ที่เพิ่งกลับจากหลับทิ้งซ็อกเก็ตค้างไว้ฝั่งบอร์ด ซึ่ง TCP
  ยังไม่รู้ว่าตายจนกว่า keepalive จะครบ ระหว่างนั้นจอค้างโดยไม่มีเหตุผลให้เห็น
- **mDNS ไม่ได้อยู่ในแกนของ IDF v5 แล้ว** — ต้องดึง `espressif/mdns` ผ่าน
  `main/idf_component.yml` การบิลด์ครั้งแรกจึงต้องมีเน็ต

**ยืนยันบนบอร์ดจริงแล้ว**: บูตผ่าน จอ init สำเร็จ NimBLE ขึ้น ไม่มี watchdog
**ยังไม่ได้ยืนยัน**: ภาพบนจอ (ต้องมองด้วยตา) · BLE จับคู่กับ daemon จริง · RGB LED

## เครื่องมือฝั่ง Python

```
tools/layout.toml          ต้นทางเดียวของค่าคงที่ layout + จานสี
tools/gen/rects.py         ชนิดข้อมูล rect list + การแปลง (เลื่อน/สะท้อน/ย่อ/ทำขอบ)
tools/gen/mascot.py        โครงร่างมาสคอต, mood, ตาราง state -> (mood, prop)
tools/gen/props.py         prop ทั้ง 9 ชิ้น + กรอบวาดมาตรฐาน
tools/gen/screen.py        ประกอบจอ 320x240 ทั้งใบ
tools/gen/render.py        rect list -> ภาพ (บีบสีเป็น RGB565 ให้ตรงกับจอจริง)
tools/preview.py           สร้าง out/states.png, out/anim/*.gif, out/screen_*.png|gif
tools/export_layout.py     layout.toml -> firmware/main/layout.h
tools/make_icon.py         โลโก้ PNG + rect list -> host/Resources/AppIcon.icns
```

**กลับคำครึ่งหนึ่ง: ไอคอนแอปขนาดใหญ่ไม่ได้มาจาก rect list แล้ว** — เดิม `make_icon.py`
เรนเดอร์มาสคอตท่า `idle` จาก `gen/mascot.py` ทุกขนาด เพื่อให้ไอคอนเปลี่ยนตามมาสคอตเอง
โดยไม่ต้องแก้สองที่ · เหตุผลที่เลิก: rect list ทำได้แค่ silhouette แบนสีเดียว ซึ่งพอดีกับจอ
320x240 ที่บีบเป็น RGB565 อยู่แล้ว แต่ไอคอนบน Dock ไปนั่งข้างไอคอนที่มีวัสดุและแสงเงา —
มาสคอตบนจอกับตราของแอปเป็นคนละงาน ไม่ใช่ภาพเดียวกันคนละขนาด
· ราคาที่จ่าย: มาสคอตเปลี่ยนแล้วไอคอนใหญ่ไม่เปลี่ยนตาม ต้องวาด PNG ใหม่เอง

**แต่ `.icns` เก็บภาพแยกต่อขนาดได้ ขนาดเล็กจึงไม่ใช่โลโก้ที่ย่อลง** — แสงเงา ฟองคำพูด
และหัวใจในโลโก้ละลายเป็นก้อนส้มที่ 16 px ของพวกนี้ไม่ได้เล็กลง มันกลายเป็นฝ้าที่ทำให้
ของที่เหลืออ่านยากขึ้น · **≤ 64 px วาดใหม่จาก rect list ตัวเดิม** เหลือแค่ตัวเครื่องสีส้ม
จอมืด มาสคอตหนึ่งตัว · **≥ 128 px ใช้ PNG** — จุดตัดอยู่ที่พิกเซลจริง ไม่ใช่ชื่อไฟล์
(`icon_16x16@2x` คือ 32 px)
- **วงคาดครีมหายต่ำกว่า 64 px** — ไม่ใช่เพราะมองไม่เห็น แต่เพราะกินที่: ส้ม→ครีม→ดำ
  สามชั้นที่ 16 px เหลือจอกว้าง 6 px ให้มาสคอตทั้งตัว
- **ตาวาดด้วยสีจอ ไม่ใช่ `PAL.ink`** — ที่ขนาดนี้ตาคือรูที่เห็นจอทะลุ หมึกที่ต่างจากพื้นจอ
  นิดเดียวกลายเป็นขอบเทาหนึ่งพิกเซลรอบรูแทนที่จะเป็นรู
- **ขอบ 10% เท่ากันทั้งสองชุด** — เคยให้ชุดเล็กบางกว่า (6%) เพื่อแลกเนื้อภาพ แต่ผู้ใช้
  เลื่อนขนาดไอคอนใน Finder ผ่านจุดตัด 64→128 แล้วเห็นไอคอนกระโดดขนาด อ่านเหมือน
  ภาพผิดมากกว่าภาพคมขึ้น · ที่ 16 px การปัดเศษทำให้ 6% กับ 10% ได้จอกว้างเท่ากันอยู่ดี
- **วาดที่ 8 เท่าแล้วย่อ** — ขอบมนที่วาดตรงๆ ที่ 16 px ได้ขอบหยัก ไม่ใช่ขอบมน

## ความเสี่ยงที่ยังเปิดอยู่

- **Python preview ≠ ของจริง** — พิสูจน์ได้แค่การออกแบบ ไม่ได้พิสูจน์ renderer ฝั่ง C
  บรรเทาด้วย `layout.toml` → `layout.h` แต่ยังไม่ได้ทดสอบครบบนบอร์ด
- **ฟอนต์ preview ≠ ฟอนต์บนบอร์ด** — ระยะห่างตัวอักษรและจุดที่ข้อความขาดจะไม่ตรงเป๊ะ
